---
phase: 02-check-in-logging-flow
plan: 01
type: execute
wave: 1
depends_on:
  - "01-03"
files_modified:
  - Sources/TempoApp/App/TempoApp.swift
  - Sources/TempoApp/App/TempoAppModel.swift
  - Sources/TempoApp/Features/CheckIn/CheckInPromptView.swift
  - Sources/TempoApp/Features/CheckIn/CheckInPromptWindowController.swift
  - Sources/TempoApp/Features/CheckIn/CheckInPromptContent.swift
  - Tests/TempoAppTests/CheckInPromptPresentationTests.swift
autonomous: true
requirements:
  - POLL-02
  - POLL-03
  - POLL-05
user_setup: []
must_haves:
  truths:
    - When the scheduler reports a due or overdue check-in, Tempo opens a prominent full-screen visual treatment without becoming a blocking modal flow.
    - The prompt surface shows the accountable elapsed duration from the scheduler and subtly indicates overdue state when applicable.
    - The overlay keeps most of the screen empty and the desktop recognizable through a dimmed backdrop.
  artifacts:
    - Sources/TempoApp/Features/CheckIn/CheckInPromptWindowController.swift
    - Sources/TempoApp/Features/CheckIn/CheckInPromptView.swift
    - Sources/TempoApp/App/TempoAppModel.swift
  key_links:
    - TempoAppModel.swift translates PollingScheduler state into a presentable CheckInPromptState.
    - CheckInPromptWindowController.swift owns the non-modal full-screen backdrop and centered prompt card presentation.
---

<objective>
Create the full-screen check-in presentation layer that turns the existing scheduler state into a visible prompt.

Purpose: Establish the visual shell and app wiring for check-ins before project selection and persistence logic are added.
Output: Check-in prompt state in the app model, a full-screen non-modal prompt window controller, prompt content view, and presentation tests.
</objective>

<execution_context>
@/Users/teo/src/tempo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/teo/src/tempo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-check-in-logging-flow/02-CONTEXT.md
@.planning/phases/01-foundation-and-timing-engine/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add app-model prompt state and scheduler-driven presentation hooks</name>
  <files>Sources/TempoApp/App/TempoAppModel.swift, Sources/TempoApp/App/TempoApp.swift</files>
  <read_first>Sources/TempoApp/App/TempoAppModel.swift, Sources/TempoApp/App/TempoApp.swift, Sources/TempoApp/Scheduler/PollingScheduler.swift, Sources/TempoApp/Models/SchedulerStateRecord.swift, .planning/phases/02-check-in-logging-flow/02-CONTEXT.md</read_first>
  <action>Extend TempoAppModel with a CheckInPromptState value that contains isPresented, elapsedDuration, isOverdue, promptTitle set to "What are you currently doing", and supportingSubtitle text derived from accountableElapsedInterval. Add app-model methods named refreshCheckInPromptState(), presentCheckInPromptIfNeeded(), and dismissCheckInPrompt() so every scheduler transition updates the prompt state. Update TempoApp.swift to instantiate a CheckInPromptWindowController once, attach it to the shared TempoAppModel, and call presentCheckInPromptIfNeeded() after performInitialLaunchIfNeeded(), handleSceneActivation(), and handleAppWake(). Keep this plan focused on presentation only: no project logging, no delay buttons, and no sounds.</action>
  <verify>rg -n "CheckInPromptState|presentCheckInPromptIfNeeded|What are you currently doing" Sources/TempoApp/App/TempoAppModel.swift Sources/TempoApp/App/TempoApp.swift</verify>
  <acceptance_criteria>
    - Sources/TempoApp/App/TempoAppModel.swift contains "struct CheckInPromptState"
    - Sources/TempoApp/App/TempoAppModel.swift contains "var isPresented: Bool"
    - Sources/TempoApp/App/TempoAppModel.swift contains "What are you currently doing"
    - Sources/TempoApp/App/TempoAppModel.swift contains "func presentCheckInPromptIfNeeded()"
    - Sources/TempoApp/App/TempoApp.swift contains "CheckInPromptWindowController"
  </acceptance_criteria>
  <done>The shared app model publishes enough prompt state for a presentation layer to open when the scheduler becomes due or overdue.</done>
</task>

<task type="auto">
  <name>Task 2: Build the non-modal full-screen backdrop and centered prompt card</name>
  <files>Sources/TempoApp/Features/CheckIn/CheckInPromptWindowController.swift, Sources/TempoApp/Features/CheckIn/CheckInPromptView.swift, Sources/TempoApp/Features/CheckIn/CheckInPromptContent.swift</files>
  <read_first>Sources/TempoApp/Features/CheckIn/CheckInPromptWindowController.swift, Sources/TempoApp/Features/CheckIn/CheckInPromptView.swift, Sources/TempoApp/Features/CheckIn/CheckInPromptContent.swift, Sources/TempoApp/App/TempoAppModel.swift, Sources/TempoApp/Views/MenuBarRootView.swift, .planning/phases/02-check-in-logging-flow/02-CONTEXT.md</read_first>
  <action>Create CheckInPromptWindowController that opens two non-modal windows on the active screen set: a borderless dimming backdrop window with alpha around 0.35 and ignoresMouseEvents set true, plus a centered floating NSPanel or borderless window for the prompt card with width near 520 points and height near 420 points. Set collectionBehavior to include .canJoinAllSpaces and .fullScreenAuxiliary so the prompt appears over full-screen apps without taking over the desktop. Create CheckInPromptView and CheckInPromptContent to render the centered card, show the prompt title, show only elapsed duration text like "Elapsed 25 min", and render overdue copy in a secondary style only when isOverdue is true. Keep most of the screen empty and do not add list or creation controls in this plan beyond placeholder regions for the next plan.</action>
  <verify>rg -n "ignoresMouseEvents|fullScreenAuxiliary|Elapsed|overdue" Sources/TempoApp/Features/CheckIn/CheckInPromptWindowController.swift Sources/TempoApp/Features/CheckIn/CheckInPromptView.swift Sources/TempoApp/Features/CheckIn/CheckInPromptContent.swift</verify>
  <acceptance_criteria>
    - Sources/TempoApp/Features/CheckIn/CheckInPromptWindowController.swift contains "ignoresMouseEvents = true"
    - Sources/TempoApp/Features/CheckIn/CheckInPromptWindowController.swift contains "fullScreenAuxiliary"
    - Sources/TempoApp/Features/CheckIn/CheckInPromptWindowController.swift contains "canJoinAllSpaces"
    - Sources/TempoApp/Features/CheckIn/CheckInPromptContent.swift contains "What are you currently doing"
    - Sources/TempoApp/Features/CheckIn/CheckInPromptContent.swift contains "Elapsed"
    - Sources/TempoApp/Features/CheckIn/CheckInPromptContent.swift contains "overdue"
  </acceptance_criteria>
  <done>The app has a concrete full-screen visual shell for check-ins that captures attention without blocking input outside the prompt card.</done>
</task>

<task type="auto">
  <name>Task 3: Add prompt presentation tests for visibility and non-blocking configuration</name>
  <files>Tests/TempoAppTests/CheckInPromptPresentationTests.swift, Sources/TempoApp/Features/CheckIn/CheckInPromptWindowController.swift</files>
  <read_first>Tests/TempoAppTests/CheckInPromptPresentationTests.swift, Sources/TempoApp/Features/CheckIn/CheckInPromptWindowController.swift, Sources/TempoApp/App/TempoAppModel.swift, Tests/TempoAppTests/PollingSchedulerTests.swift</read_first>
  <action>Create CheckInPromptPresentationTests that instantiate CheckInPromptWindowController with test doubles and verify three explicit properties: the backdrop window covers NSScreen.frame dimensions, the backdrop window ignores mouse events, and the prompt window uses a non-modal floating level with full-screen auxiliary collection behavior. Add a formatting test that turns an accountableElapsedInterval of 25 * 60 into prompt text containing "Elapsed 25 min". Keep the tests deterministic and limited to state/configuration checks, not live UI automation.</action>
  <verify>rg -n "testBackdropIgnoresMouseEvents|testPromptUsesFullScreenAuxiliary|Elapsed 25 min" Tests/TempoAppTests/CheckInPromptPresentationTests.swift</verify>
  <acceptance_criteria>
    - Tests/TempoAppTests/CheckInPromptPresentationTests.swift contains "final class CheckInPromptPresentationTests"
    - Tests/TempoAppTests/CheckInPromptPresentationTests.swift contains "func testBackdropIgnoresMouseEvents()"
    - Tests/TempoAppTests/CheckInPromptPresentationTests.swift contains "func testPromptUsesFullScreenAuxiliary()"
    - Tests/TempoAppTests/CheckInPromptPresentationTests.swift contains "Elapsed 25 min"
  </acceptance_criteria>
  <done>The prompt presentation contract is covered by deterministic tests that protect the full-screen but non-blocking behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Prompt state is exposed from TempoAppModel and updates from scheduler transitions
- [ ] The prompt uses a dimmed full-screen backdrop plus centered card presentation
- [ ] The backdrop ignores mouse events and the prompt uses full-screen auxiliary window behavior
- [ ] Tests cover the non-blocking presentation configuration
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Tempo can present a full-screen check-in shell without stealing system input outside the prompt controls
- The prompt shows elapsed-duration context and subtle overdue messaging ready for project selection work
  </success_criteria>

<output>
After completion, create `.planning/phases/02-check-in-logging-flow/02-01-SUMMARY.md`
</output>
