---
phase: 02-check-in-logging-flow
plan: 03
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
  - "01-03"
files_modified:
  - Sources/TempoApp/App/TempoAppModel.swift
  - Sources/TempoApp/Scheduler/PollingScheduler.swift
  - Sources/TempoApp/Scheduler/SchedulerStateStore.swift
  - Sources/TempoApp/Models/TimeEntryRecord.swift
  - Sources/TempoApp/Features/CheckIn/CheckInPromptContent.swift
  - Tests/TempoAppTests/CheckInCompletionTests.swift
  - Tests/TempoAppTests/PollingSchedulerTests.swift
autonomous: true
requirements:
  - POLL-06
  - UX-01
  - PROJ-02
  - PROJ-03
user_setup: []
must_haves:
  truths:
    - Selecting an existing project or creating a new one from the prompt immediately writes a time entry for the elapsed accountable interval and closes the prompt.
    - Completing a check-in updates scheduler state so the next prompt schedules from the completion time, not the stale due time.
    - The completion flow stays silent and does not invoke audio notifications or confirmation UI.
  artifacts:
    - Sources/TempoApp/App/TempoAppModel.swift
    - Sources/TempoApp/Scheduler/PollingScheduler.swift
    - Tests/TempoAppTests/CheckInCompletionTests.swift
  key_links:
    - TempoAppModel.swift persists TimeEntryRecord and then applies the scheduler completion result before dismissing the prompt.
    - PollingScheduler.swift gains an explicit completion API that resets overdue state and schedules the next check-in from the completion timestamp.
---

<objective>
Persist completed check-ins and reset the scheduler cleanly after project selection.

Purpose: Finish the core Phase 2 user outcome so every prompt selection turns into a trustworthy time block without sound or extra confirmation.
Output: Prompt completion methods in the app model, scheduler completion semantics, persisted time-entry writes, and completion tests.
</objective>

<execution_context>
@/Users/teo/src/tempo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/teo/src/tempo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-check-in-logging-flow/02-CONTEXT.md
@.planning/phases/02-check-in-logging-flow/02-01-SUMMARY.md
@.planning/phases/02-check-in-logging-flow/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit scheduler completion semantics for finished check-ins</name>
  <files>Sources/TempoApp/Scheduler/PollingScheduler.swift, Sources/TempoApp/Scheduler/SchedulerStateStore.swift</files>
  <read_first>Sources/TempoApp/Scheduler/PollingScheduler.swift, Sources/TempoApp/Scheduler/SchedulerStateStore.swift, Sources/TempoApp/Models/SchedulerStateRecord.swift, Sources/TempoApp/Models/AppSettingsRecord.swift, Tests/TempoAppTests/PollingSchedulerTests.swift, .planning/phases/02-check-in-logging-flow/02-CONTEXT.md</read_first>
  <action>Add a PollingScheduler completion method named completeCheckIn(state:settings:completionDate:) that sets lastCheckInAt to completionDate, sets nextCheckInAt to completionDate plus settings.pollingIntervalMinutes * 60, and returns a snapshot with isPromptOverdue false and accountableElapsedInterval equal to settings.pollingIntervalMinutes * 60. Update SchedulerStateStore if needed so the completion path writes the same SchedulerStateRecord singleton used by launch and wake updates. Do not carry overdue status forward after a completed check-in.</action>
  <verify>rg -n "completeCheckIn|lastCheckInAt = completionDate|isPromptOverdue: false" Sources/TempoApp/Scheduler/PollingScheduler.swift Sources/TempoApp/Scheduler/SchedulerStateStore.swift</verify>
  <acceptance_criteria>
    - Sources/TempoApp/Scheduler/PollingScheduler.swift contains "func completeCheckIn"
    - Sources/TempoApp/Scheduler/PollingScheduler.swift contains "lastCheckInAt: completionDate"
    - Sources/TempoApp/Scheduler/PollingScheduler.swift contains "isPromptOverdue: false"
    - Sources/TempoApp/Scheduler/PollingScheduler.swift contains "pollingIntervalMinutes * 60"
  </acceptance_criteria>
  <done>The scheduler exposes a concrete completion path that resets prompt state and schedules the next check-in from the actual completion time.</done>
</task>

<task type="auto">
  <name>Task 2: Persist time entries for existing and inline-created projects, then close the prompt</name>
  <files>Sources/TempoApp/App/TempoAppModel.swift, Sources/TempoApp/Models/TimeEntryRecord.swift, Sources/TempoApp/Features/CheckIn/CheckInPromptContent.swift</files>
  <read_first>Sources/TempoApp/App/TempoAppModel.swift, Sources/TempoApp/Models/TimeEntryRecord.swift, Sources/TempoApp/Features/CheckIn/CheckInPromptContent.swift, Sources/TempoApp/Models/ProjectRecord.swift, Sources/TempoApp/Scheduler/PollingScheduler.swift, .planning/phases/02-check-in-logging-flow/02-CONTEXT.md</read_first>
  <action>Extend TempoAppModel with selectProjectForPrompt(_:) and createAndSelectProjectForPrompt(named:) methods. When either method runs, compute entryEndAt as clock.now and entryStartAt as entryEndAt minus accountableElapsedInterval, then insert a TimeEntryRecord with project set to the selected or newly created ProjectRecord, startAt set to entryStartAt, endAt set to entryEndAt, and source set exactly to "check-in". After saving the model context, call the new scheduler completeCheckIn method, clear promptSearchText, dismiss the prompt immediately, and do not show a confirmation banner, sheet, alert, NSBeep, or NSSound call.</action>
  <verify>rg -n "selectProjectForPrompt|createAndSelectProjectForPrompt|source: \"check-in\"|dismissCheckInPrompt" Sources/TempoApp/App/TempoAppModel.swift Sources/TempoApp/Features/CheckIn/CheckInPromptContent.swift</verify>
  <acceptance_criteria>
    - Sources/TempoApp/App/TempoAppModel.swift contains "func selectProjectForPrompt"
    - Sources/TempoApp/App/TempoAppModel.swift contains "func createAndSelectProjectForPrompt"
    - Sources/TempoApp/App/TempoAppModel.swift contains "source: \"check-in\""
    - Sources/TempoApp/App/TempoAppModel.swift contains "promptSearchText = \"\""
    - Sources/TempoApp/App/TempoAppModel.swift does not contain "NSSound"
    - Sources/TempoApp/App/TempoAppModel.swift does not contain "NSBeep"
  </acceptance_criteria>
  <done>The check-in flow writes a concrete time block and closes immediately for both existing-project and inline-created-project paths.</done>
</task>

<task type="auto">
  <name>Task 3: Add completion tests for time-entry persistence, scheduler reset, and silence</name>
  <files>Tests/TempoAppTests/CheckInCompletionTests.swift, Tests/TempoAppTests/PollingSchedulerTests.swift, Sources/TempoApp/App/TempoAppModel.swift</files>
  <read_first>Tests/TempoAppTests/CheckInCompletionTests.swift, Tests/TempoAppTests/PollingSchedulerTests.swift, Sources/TempoApp/App/TempoAppModel.swift, Sources/TempoApp/Scheduler/PollingScheduler.swift, Sources/TempoApp/Models/TimeEntryRecord.swift, Sources/TempoApp/Models/ProjectRecord.swift</read_first>
  <action>Create CheckInCompletionTests with an in-memory model container and fixed clock. Add one test that selecting an existing project inserts exactly one TimeEntryRecord whose duration matches accountableElapsedInterval and whose source is "check-in". Add one test that createAndSelectProjectForPrompt(named:) inserts a new ProjectRecord and linked TimeEntryRecord in one action. Extend PollingSchedulerTests with a test proving completeCheckIn schedules the next check-in from the completion date. Add a static source-level assertion that the completion flow files do not contain NSSound or NSBeep references so UX-01 remains enforced.</action>
  <verify>rg -n "testSelectingExistingProjectCreatesTimeEntry|testCreateAndSelectProjectPersistsProjectAndEntry|testCompleteCheckInSchedulesFromCompletionDate|NSBeep|NSSound" Tests/TempoAppTests/CheckInCompletionTests.swift Tests/TempoAppTests/PollingSchedulerTests.swift</verify>
  <acceptance_criteria>
    - Tests/TempoAppTests/CheckInCompletionTests.swift contains "final class CheckInCompletionTests"
    - Tests/TempoAppTests/CheckInCompletionTests.swift contains "func testSelectingExistingProjectCreatesTimeEntry()"
    - Tests/TempoAppTests/CheckInCompletionTests.swift contains "func testCreateAndSelectProjectPersistsProjectAndEntry()"
    - Tests/TempoAppTests/PollingSchedulerTests.swift contains "func testCompleteCheckInSchedulesFromCompletionDate()"
    - Tests/TempoAppTests/CheckInCompletionTests.swift contains "NSSound"
    - Tests/TempoAppTests/CheckInCompletionTests.swift contains "NSBeep"
  </acceptance_criteria>
  <done>The completed check-in path is covered by deterministic persistence and scheduler tests, with a guard against introducing audio cues.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Selecting a project writes a TimeEntryRecord with source "check-in"
- [ ] Inline creation writes both the new project and its linked time entry in one flow
- [ ] Completing a check-in resets overdue state and schedules the next prompt from completion time
- [ ] Tests cover existing-project completion, inline-created completion, scheduler completion semantics, and the no-sound constraint
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Tempo logs elapsed work correctly when a user completes a check-in
- The prompt closes immediately and the app remains silent throughout the completion flow
  </success_criteria>

<output>
After completion, create `.planning/phases/02-check-in-logging-flow/02-03-SUMMARY.md`
</output>
