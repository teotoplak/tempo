---
phase: 02-check-in-logging-flow
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
  - "01-02"
files_modified:
  - Sources/TempoApp/App/TempoAppModel.swift
  - Sources/TempoApp/Features/CheckIn/CheckInPromptContent.swift
  - Sources/TempoApp/Features/CheckIn/CheckInProjectListView.swift
  - Sources/TempoApp/Features/CheckIn/InlineProjectCreationView.swift
  - Sources/TempoApp/Models/ProjectRecord.swift
  - Tests/TempoAppTests/CheckInProjectSelectionTests.swift
autonomous: true
requirements:
  - PROJ-02
  - PROJ-03
  - POLL-04
user_setup: []
must_haves:
  truths:
    - The check-in prompt shows the flat project list, with recently used projects pinned first and the remaining projects still accessible in one vertical list.
    - A single text field above the list filters project names in real time and doubles as the inline new-project entry point.
    - Creating a non-empty trimmed project name from the prompt uses the same persistence path as the main project-management screen.
  artifacts:
    - Sources/TempoApp/Features/CheckIn/CheckInProjectListView.swift
    - Sources/TempoApp/Features/CheckIn/InlineProjectCreationView.swift
    - Sources/TempoApp/App/TempoAppModel.swift
  key_links:
    - TempoAppModel.swift computes recent-project ordering from completed TimeEntryRecord values.
    - CheckInPromptContent.swift composes the search field, create action, and project list in a single centered card.
---

<objective>
Build project selection and inline project creation inside the check-in prompt.

Purpose: Turn the presentation shell into a usable classification flow while preserving the flat-list simplicity established in Phase 1.
Output: Prompt search/filter state, recent-project ordering, inline create affordance, project selection list UI, and prompt selection tests.
</objective>

<execution_context>
@/Users/teo/src/tempo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/teo/src/tempo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-check-in-logging-flow/02-CONTEXT.md
@.planning/phases/01-foundation-and-timing-engine/01-02-SUMMARY.md
@.planning/phases/02-check-in-logging-flow/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add prompt query state, recent-project ordering, and filter logic</name>
  <files>Sources/TempoApp/App/TempoAppModel.swift, Sources/TempoApp/Models/ProjectRecord.swift</files>
  <read_first>Sources/TempoApp/App/TempoAppModel.swift, Sources/TempoApp/Models/ProjectRecord.swift, Sources/TempoApp/Models/TimeEntryRecord.swift, Sources/TempoApp/Features/Projects/ProjectManagementView.swift, .planning/phases/02-check-in-logging-flow/02-CONTEXT.md</read_first>
  <action>Extend TempoAppModel with promptSearchText, filteredPromptProjects, recentPromptProjects, and canCreatePromptProject(named:) helpers. Compute recently used ordering from TimeEntryRecord values by sorting projects with the most recent linked endAt first, then append untouched projects by ProjectRecord.sortOrder. Keep filtering case-insensitive against project.name and preserve the flat single-list model with no grouping headers or hierarchy. Add a ProjectRecord helper or computed property only if needed to keep list-row identity and recent ordering stable.</action>
  <verify>rg -n "promptSearchText|filteredPromptProjects|recentPromptProjects|canCreatePromptProject" Sources/TempoApp/App/TempoAppModel.swift</verify>
  <acceptance_criteria>
    - Sources/TempoApp/App/TempoAppModel.swift contains "var promptSearchText = \"\""
    - Sources/TempoApp/App/TempoAppModel.swift contains "var filteredPromptProjects"
    - Sources/TempoApp/App/TempoAppModel.swift contains "var recentPromptProjects"
    - Sources/TempoApp/App/TempoAppModel.swift contains "localizedCaseInsensitiveContains"
    - Sources/TempoApp/App/TempoAppModel.swift contains "endAt"
  </acceptance_criteria>
  <done>The app model can drive a check-in list that filters live and prioritizes recently used projects without changing the flat project model.</done>
</task>

<task type="auto">
  <name>Task 2: Build the single-field filter-and-create prompt UI</name>
  <files>Sources/TempoApp/Features/CheckIn/CheckInPromptContent.swift, Sources/TempoApp/Features/CheckIn/CheckInProjectListView.swift, Sources/TempoApp/Features/CheckIn/InlineProjectCreationView.swift</files>
  <read_first>Sources/TempoApp/Features/CheckIn/CheckInPromptContent.swift, Sources/TempoApp/Features/CheckIn/CheckInProjectListView.swift, Sources/TempoApp/Features/CheckIn/InlineProjectCreationView.swift, Sources/TempoApp/App/TempoAppModel.swift, Sources/TempoApp/Models/ProjectRecord.swift, .planning/phases/02-check-in-logging-flow/02-CONTEXT.md</read_first>
  <action>Update CheckInPromptContent to place a single TextField above the project list and bind it to appModel.promptSearchText so typing starts filtering immediately. Create CheckInProjectListView as a simple vertical list of tappable rows that display project.name only and wire each row to a selectProjectForPrompt(_:) callback. Create InlineProjectCreationView that appears next to or directly beneath the text field when the trimmed search value does not exactly match an existing project name, with button copy formatted as "Create \"{name}\"". Do not add confirmation dialogs, project metadata, or separate create screens.</action>
  <verify>rg -n "TextField|Create \\\\\"|project.name|selectProjectForPrompt" Sources/TempoApp/Features/CheckIn/CheckInPromptContent.swift Sources/TempoApp/Features/CheckIn/CheckInProjectListView.swift Sources/TempoApp/Features/CheckIn/InlineProjectCreationView.swift</verify>
  <acceptance_criteria>
    - Sources/TempoApp/Features/CheckIn/CheckInPromptContent.swift contains "TextField"
    - Sources/TempoApp/Features/CheckIn/CheckInPromptContent.swift contains "promptSearchText"
    - Sources/TempoApp/Features/CheckIn/CheckInProjectListView.swift contains "project.name"
    - Sources/TempoApp/Features/CheckIn/InlineProjectCreationView.swift contains "Create \\(\""
    - Sources/TempoApp/Features/CheckIn/InlineProjectCreationView.swift does not contain "sheet("
  </acceptance_criteria>
  <done>The prompt provides a single-field flow where the user can either find an existing project or create a new one inline.</done>
</task>

<task type="auto">
  <name>Task 3: Add prompt-selection tests for filtering, recents, and create visibility</name>
  <files>Tests/TempoAppTests/CheckInProjectSelectionTests.swift, Sources/TempoApp/App/TempoAppModel.swift</files>
  <read_first>Tests/TempoAppTests/CheckInProjectSelectionTests.swift, Sources/TempoApp/App/TempoAppModel.swift, Sources/TempoApp/Models/ProjectRecord.swift, Sources/TempoApp/Models/TimeEntryRecord.swift, Tests/TempoAppTests/PersistenceModelTests.swift</read_first>
  <action>Create CheckInProjectSelectionTests using an in-memory model container with at least three projects and two completed TimeEntryRecord values. Add one test proving the most recently used project sorts first, one test proving promptSearchText filters case-insensitively, and one test proving canCreatePromptProject(named:) returns true for a non-empty trimmed unmatched value and false for an exact match or whitespace-only value. Keep the tests on model behavior so the next execution phase can trust the prompt ordering and create visibility rules.</action>
  <verify>rg -n "testRecentProjectsSortFirst|testPromptFilteringIsCaseInsensitive|testCanCreatePromptProjectOnlyForUnmatchedTrimmedName" Tests/TempoAppTests/CheckInProjectSelectionTests.swift</verify>
  <acceptance_criteria>
    - Tests/TempoAppTests/CheckInProjectSelectionTests.swift contains "final class CheckInProjectSelectionTests"
    - Tests/TempoAppTests/CheckInProjectSelectionTests.swift contains "func testRecentProjectsSortFirst()"
    - Tests/TempoAppTests/CheckInProjectSelectionTests.swift contains "func testPromptFilteringIsCaseInsensitive()"
    - Tests/TempoAppTests/CheckInProjectSelectionTests.swift contains "func testCanCreatePromptProjectOnlyForUnmatchedTrimmedName()"
  </acceptance_criteria>
  <done>The project-picking rules are protected by deterministic tests before logging behavior is added.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] The prompt shows a vertical project list with recently used projects pinned first
- [ ] Typing in one text field filters the project list in real time
- [ ] An inline create action appears only for non-empty trimmed unmatched names
- [ ] Tests cover recents ordering, filtering, and create eligibility
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Tempo can present and filter existing projects during a check-in
- Tempo can prepare inline project creation from the same prompt field without leaving the popup
  </success_criteria>

<output>
After completion, create `.planning/phases/02-check-in-logging-flow/02-02-SUMMARY.md`
</output>
