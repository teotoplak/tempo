---
phase: 01-foundation-and-timing-engine
plan: 03
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - Sources/TempoApp/Scheduler/PollingScheduler.swift
  - Sources/TempoApp/Scheduler/SchedulerClock.swift
  - Sources/TempoApp/Scheduler/SchedulerStateStore.swift
  - Sources/TempoApp/App/TempoAppModel.swift
  - Sources/TempoApp/App/TempoApp.swift
  - Sources/TempoApp/Views/MenuBarRootView.swift
  - Tests/TempoAppTests/PollingSchedulerTests.swift
autonomous: true
requirements:
  - POLL-01
user_setup: []
must_haves:
  truths:
    - The app computes the first check-in exactly one configured interval after launch.
    - Wake or relaunch triggers an immediate overdue prompt signal when a scheduled check-in was missed.
    - Scheduler timing state persists locally and survives app restarts.
  artifacts:
    - Sources/TempoApp/Scheduler/PollingScheduler.swift
    - Sources/TempoApp/Scheduler/SchedulerStateStore.swift
    - Tests/TempoAppTests/PollingSchedulerTests.swift
  key_links:
    - PollingScheduler reads AppSettingsRecord values and writes SchedulerStateRecord updates through SchedulerStateStore.
    - TempoAppModel publishes scheduler state changes to MenuBarRootView and future prompt surfaces.
---

<objective>
Implement the dependable polling engine and persisted timing state that define the product’s core value.

Purpose: Lock down the scheduling semantics now so later popup and idle-handling phases inherit trustworthy time calculations instead of inventing them ad hoc.
Output: Polling scheduler service, persisted scheduler-state store, lifecycle wiring for launch and wake handling, and scheduler tests.
</objective>

<execution_context>
@/Users/teo/src/tempo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/teo/src/tempo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-timing-engine/01-CONTEXT.md
@.planning/phases/01-foundation-and-timing-engine/01-01-SUMMARY.md
@.planning/phases/01-foundation-and-timing-engine/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the persisted polling scheduler core</name>
  <files>Sources/TempoApp/Scheduler/PollingScheduler.swift, Sources/TempoApp/Scheduler/SchedulerClock.swift, Sources/TempoApp/Scheduler/SchedulerStateStore.swift, Sources/TempoApp/App/TempoAppModel.swift</files>
  <read_first>Sources/TempoApp/Scheduler/PollingScheduler.swift, Sources/TempoApp/Scheduler/SchedulerClock.swift, Sources/TempoApp/Scheduler/SchedulerStateStore.swift, Sources/TempoApp/App/TempoAppModel.swift, Sources/TempoApp/Models/AppSettingsRecord.swift, Sources/TempoApp/Models/SchedulerStateRecord.swift, .planning/phases/01-foundation-and-timing-engine/01-CONTEXT.md</read_first>
  <action>Create PollingScheduler that accepts a SchedulerClock protocol and computes nextCheckInAt from three explicit rules: first launch schedules now + pollingIntervalMinutes, relaunch or wake with overdue nextCheckInAt marks the prompt as overdue immediately, and relaunch or wake without overdue nextCheckInAt preserves the existing schedule. Create SchedulerStateStore to load and save SchedulerStateRecord through SwiftData. Update TempoAppModel to initialize the scheduler, persist lastAppLaunchAt, and publish scheduler status values such as nextCheckInAt, isPromptOverdue, and accountableElapsedInterval.</action>
  <verify>swift build</verify>
  <acceptance_criteria>
    - Sources/TempoApp/Scheduler/PollingScheduler.swift contains "protocol SchedulerClock"
    - Sources/TempoApp/Scheduler/PollingScheduler.swift contains "isPromptOverdue"
    - Sources/TempoApp/Scheduler/PollingScheduler.swift contains "pollingIntervalMinutes"
    - Sources/TempoApp/Scheduler/SchedulerStateStore.swift contains "SchedulerStateRecord"
    - Sources/TempoApp/App/TempoAppModel.swift contains "nextCheckInAt"
  </acceptance_criteria>
  <done>The app compiles with a scheduler core that persists timing state and exposes overdue/next-check-in information through TempoAppModel.</done>
</task>

<task type="auto">
  <name>Task 2: Wire launch and wake behavior into the app lifecycle</name>
  <files>Sources/TempoApp/App/TempoApp.swift, Sources/TempoApp/App/TempoAppModel.swift, Sources/TempoApp/Views/MenuBarRootView.swift</files>
  <read_first>Sources/TempoApp/App/TempoApp.swift, Sources/TempoApp/App/TempoAppModel.swift, Sources/TempoApp/Views/MenuBarRootView.swift, Sources/TempoApp/Scheduler/PollingScheduler.swift</read_first>
  <action>Update TempoApp.swift and TempoAppModel.swift to call the scheduler on app launch, scene activation, and wake notifications from NSWorkspace. Keep the app launching quietly into the menu bar without automatically opening the main window or popover. In MenuBarRootView, display scheduler state needed for Phase 1 diagnostics only: next check-in timestamp and whether the app currently considers a prompt overdue. Do not implement the actual prompt UI or the Phase 3 countdown display in this plan.</action>
  <verify>swift build</verify>
  <acceptance_criteria>
    - Sources/TempoApp/App/TempoApp.swift contains "NSWorkspace.didWakeNotification"
    - Sources/TempoApp/App/TempoAppModel.swift contains "handleAppWake"
    - Sources/TempoApp/Views/MenuBarRootView.swift contains "Next check-in"
    - Sources/TempoApp/Views/MenuBarRootView.swift contains "Overdue"
    - Sources/TempoApp/App/TempoApp.swift does not contain "openWindow(id:"
  </acceptance_criteria>
  <done>The scheduler is connected to launch and wake lifecycle events while the app still starts quietly in the menu bar.</done>
</task>

<task type="auto">
  <name>Task 3: Add deterministic scheduler tests for launch, relaunch, and overdue cases</name>
  <files>Tests/TempoAppTests/PollingSchedulerTests.swift, Sources/TempoApp/Scheduler/PollingScheduler.swift</files>
  <read_first>Tests/TempoAppTests/PollingSchedulerTests.swift, Sources/TempoApp/Scheduler/PollingScheduler.swift, Sources/TempoApp/Models/AppSettingsRecord.swift, Sources/TempoApp/Models/SchedulerStateRecord.swift</read_first>
  <action>Create PollingSchedulerTests with a fake clock and explicit cases for first launch scheduling, non-overdue relaunch preserving the existing nextCheckInAt, overdue relaunch setting isPromptOverdue true, and elapsed interval calculations using the configured polling interval of 25 minutes. Keep the assertions deterministic by passing fixed Date values into the scheduler rather than using Date.now in tests.</action>
  <verify>swift test</verify>
  <acceptance_criteria>
    - Tests/TempoAppTests/PollingSchedulerTests.swift contains "func testFirstLaunchSchedulesFromNow()"
    - Tests/TempoAppTests/PollingSchedulerTests.swift contains "func testRelaunchPreservesFutureCheckIn()"
    - Tests/TempoAppTests/PollingSchedulerTests.swift contains "func testOverdueWakeMarksPromptOverdue()"
    - Tests/TempoAppTests/PollingSchedulerTests.swift contains "25 * 60"
  </acceptance_criteria>
  <done>swift test succeeds and the scheduler semantics are covered by deterministic launch/relaunch/overdue tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] swift build succeeds
- [ ] swift test succeeds
- [ ] Scheduler tests cover first launch, relaunch, and overdue wake behavior
- [ ] The app still launches quietly without opening the main window automatically
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The scheduler computes Phase 1 timing semantics and persists them locally
- Future prompt phases can consume overdue and next-check-in state without reworking the timing model
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-timing-engine/01-03-SUMMARY.md`
</output>
