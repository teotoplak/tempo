---
phase: 01-foundation-and-timing-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Package.swift
  - Sources/TempoApp/App/TempoApp.swift
  - Sources/TempoApp/App/TempoAppModel.swift
  - Sources/TempoApp/Views/MenuBarRootView.swift
  - Sources/TempoApp/Views/AppWindowShellView.swift
  - Sources/TempoApp/Support/AppSceneID.swift
  - Tests/TempoAppTests/TempoAppBootstrapTests.swift
autonomous: true
requirements:
  - MENU-01
  - DATA-02
user_setup: []
must_haves:
  truths:
    - The repository builds as a native macOS SwiftUI app with a menu bar entry point.
    - The app provides both a MenuBarExtra surface and a reusable main window shell without requiring network services.
    - The baseline app state object is available to later persistence and scheduler plans.
  artifacts:
    - Package.swift
    - Sources/TempoApp/App/TempoApp.swift
    - Sources/TempoApp/App/TempoAppModel.swift
    - Sources/TempoApp/Views/MenuBarRootView.swift
    - Sources/TempoApp/Views/AppWindowShellView.swift
  key_links:
    - TempoApp.swift constructs MenuBarRootView and AppWindowShellView from a shared TempoAppModel instance.
    - Package.swift defines a macOS executable target and test target for TempoApp.
---

<objective>
Create the native macOS app scaffold that later plans can extend with persistence, project management, and scheduler logic.

Purpose: Establish a buildable SwiftUI menu bar application with a reusable window shell and shared app model so the rest of Phase 1 has a stable foundation.
Output: Swift package manifest, app entry point, menu bar root view, general window shell, and bootstrap tests.
</objective>

<execution_context>
@/Users/teo/src/tempo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/teo/src/tempo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-timing-engine/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the macOS Swift package and app entry point</name>
  <files>Package.swift, Sources/TempoApp/App/TempoApp.swift, Sources/TempoApp/Support/AppSceneID.swift</files>
  <read_first>Package.swift, Sources/TempoApp/App/TempoApp.swift, Sources/TempoApp/Support/AppSceneID.swift, .planning/PROJECT.md, .planning/ROADMAP.md, .planning/phases/01-foundation-and-timing-engine/01-CONTEXT.md</read_first>
  <action>Create Package.swift with swift-tools-version 6.0, package name TempoApp, platform .macOS(.v15), one executable target named TempoApp, and one test target named TempoAppTests. Create Sources/TempoApp/App/TempoApp.swift with @main struct TempoApp: App that builds a MenuBarExtra titled "Tempo" with systemImage "clock" and a Window titled "Tempo" using AppSceneID.mainWindow. Keep the app local-only by not adding any package dependencies, network frameworks, or remote URLs to Package.swift.</action>
  <verify>swift build</verify>
  <acceptance_criteria>
    - Package.swift contains "platforms: [.macOS(.v15)]"
    - Package.swift contains ".executableTarget(" and "name: \"TempoApp\""
    - Sources/TempoApp/App/TempoApp.swift contains "@main struct TempoApp: App"
    - Sources/TempoApp/App/TempoApp.swift contains "MenuBarExtra(\"Tempo\", systemImage: \"clock\")"
    - Sources/TempoApp/App/TempoApp.swift contains "Window(\"Tempo\""
  </acceptance_criteria>
  <done>swift build succeeds and the codebase contains a native macOS menu bar app entry point with a reusable window scene.</done>
</task>

<task type="auto">
  <name>Task 2: Introduce shared app model and minimal shell views</name>
  <files>Sources/TempoApp/App/TempoAppModel.swift, Sources/TempoApp/Views/MenuBarRootView.swift, Sources/TempoApp/Views/AppWindowShellView.swift</files>
  <read_first>Sources/TempoApp/App/TempoAppModel.swift, Sources/TempoApp/Views/MenuBarRootView.swift, Sources/TempoApp/Views/AppWindowShellView.swift, Sources/TempoApp/App/TempoApp.swift, .planning/phases/01-foundation-and-timing-engine/01-CONTEXT.md</read_first>
  <action>Create Sources/TempoApp/App/TempoAppModel.swift with @MainActor @Observable final class TempoAppModel containing shell-level properties for selectedWindow, launchState, and placeholders for projectStore, settingsStore, and schedulerStore injection. Create Sources/TempoApp/Views/MenuBarRootView.swift with a minimal SwiftUI view that shows app status plus buttons to open the main window and quit. Create Sources/TempoApp/Views/AppWindowShellView.swift with a simple navigation split or stacked shell that reserves sections for Projects and Future Analytics, matching the decision that analytics will later live in a real app window. Wire both views to consume the shared TempoAppModel instance created in TempoApp.swift.</action>
  <verify>swift build</verify>
  <acceptance_criteria>
    - Sources/TempoApp/App/TempoAppModel.swift contains "@Observable final class TempoAppModel"
    - Sources/TempoApp/Views/MenuBarRootView.swift contains "Button(\"Open Tempo\")"
    - Sources/TempoApp/Views/MenuBarRootView.swift contains "Button(\"Quit Tempo\")"
    - Sources/TempoApp/Views/AppWindowShellView.swift contains "Projects"
    - Sources/TempoApp/Views/AppWindowShellView.swift contains "Analytics"
  </acceptance_criteria>
  <done>The app has a shared model and both menu bar and window shell views build against it successfully.</done>
</task>

<task type="auto">
  <name>Task 3: Add bootstrap tests and enforce local-only foundation</name>
  <files>Tests/TempoAppTests/TempoAppBootstrapTests.swift, Package.swift</files>
  <read_first>Tests/TempoAppTests/TempoAppBootstrapTests.swift, Package.swift, Sources/TempoApp/App/TempoApp.swift, Sources/TempoApp/App/TempoAppModel.swift</read_first>
  <action>Create Tests/TempoAppTests/TempoAppBootstrapTests.swift with at least one test that instantiates TempoAppModel and one manifest-level assertion helper or fixture proving the package builds without external dependencies. Keep Package.swift free of .package(url:) declarations, URLSession imports, and network-specific package products so the foundation remains local-only.</action>
  <verify>swift test</verify>
  <acceptance_criteria>
    - Tests/TempoAppTests/TempoAppBootstrapTests.swift contains "final class TempoAppBootstrapTests"
    - Tests/TempoAppTests/TempoAppBootstrapTests.swift contains "func testTempoAppModelBootstraps()"
    - Package.swift does not contain ".package(url:"
  </acceptance_criteria>
  <done>swift test succeeds and the scaffold is verifiably local-only with no external service dependency.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] swift build succeeds
- [ ] swift test succeeds
- [ ] Package.swift contains a macOS executable target and no remote package URLs
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repository contains a buildable native menu bar app shell with a reusable main window scene
- The codebase remains local-only with no networking dependency added
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-timing-engine/01-01-SUMMARY.md`
</output>
