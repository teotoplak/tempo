---
phase: 03-deferrals-silence-and-menu-bar-control
plan: 01
type: execute
wave: 1
depends_on:
  - "02-03"
files_modified:
  - Sources/TempoApp/App/TempoAppModel.swift
  - Sources/TempoApp/Scheduler/PollingScheduler.swift
  - Sources/TempoApp/Scheduler/SchedulerStateStore.swift
  - Sources/TempoApp/Models/SchedulerStateRecord.swift
  - Sources/TempoApp/Features/CheckIn/CheckInPromptContent.swift
  - Tests/TempoAppTests/PollingSchedulerTests.swift
  - Tests/TempoAppTests/CheckInCompletionTests.swift
autonomous: true
requirements:
  - POLL-07
  - DELY-01
  - DELY-03
must_haves:
  truths:
    - A due check-in can be postponed from the prompt using the configured delay presets without writing a time entry.
    - Delaying hides the prompt immediately and schedules the next prompt from the chosen delay duration.
    - When the delay expires, Tempo shows the prompt again with elapsed time still covering the full accountable block.
  artifacts:
    - Sources/TempoApp/Scheduler/PollingScheduler.swift
    - Sources/TempoApp/App/TempoAppModel.swift
    - Sources/TempoApp/Features/CheckIn/CheckInPromptContent.swift
  key_links:
    - PollingScheduler.swift owns the canonical delay reschedule semantics so prompt actions and lifecycle refreshes stay consistent.
    - TempoAppModel.swift exposes prompt delay actions and applies the scheduler result through SchedulerStateStore.
---

<objective>
Implement prompt delay actions and rescheduling semantics.

Purpose: Let the user defer a due check-in without losing the current accountable work block or forcing an immediate classification.
Output: Delay-aware scheduler state, prompt actions for configured presets, and deterministic tests for postponement behavior.
</objective>

<execution_context>
@/Users/teo/src/tempo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/teo/src/tempo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-check-in-logging-flow/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend scheduler state with explicit delay semantics</name>
  <files>Sources/TempoApp/Scheduler/PollingScheduler.swift, Sources/TempoApp/Scheduler/SchedulerStateStore.swift, Sources/TempoApp/Models/SchedulerStateRecord.swift</files>
  <read_first>Sources/TempoApp/Scheduler/PollingScheduler.swift, Sources/TempoApp/Scheduler/SchedulerStateStore.swift, Sources/TempoApp/Models/SchedulerStateRecord.swift, Sources/TempoApp/Models/AppSettingsRecord.swift, Tests/TempoAppTests/PollingSchedulerTests.swift</read_first>
  <action>Add persisted delay fields to SchedulerStateRecord for the active deferral window, such as delayedUntilAt and delayedFromPromptAt. Extend PollingSchedulerSnapshot and PollingSchedulerResult to carry whether the prompt is currently delayed. Add a scheduler API named delayCheckIn(state:settings:delayMinutes:delayDate:) that moves nextCheckInAt to delayDate plus the chosen delay, preserves the original accountable start reference so elapsed time can continue growing after the delay, and marks the prompt as not overdue while the delay window is active. Update updateState so once delayedUntilAt is reached, the returned snapshot becomes due again instead of silently scheduling a fresh baseline.</action>
  <verify>rg -n "delayedUntilAt|delayCheckIn|isPromptDelayed" Sources/TempoApp/Scheduler/PollingScheduler.swift Sources/TempoApp/Scheduler/SchedulerStateStore.swift Sources/TempoApp/Models/SchedulerStateRecord.swift</verify>
  <acceptance_criteria>
    - Sources/TempoApp/Models/SchedulerStateRecord.swift contains "delayedUntilAt"
    - Sources/TempoApp/Scheduler/PollingScheduler.swift contains "func delayCheckIn"
    - Sources/TempoApp/Scheduler/PollingScheduler.swift contains "isPromptDelayed"
    - Sources/TempoApp/Scheduler/PollingScheduler.swift contains "delayMinutes"
  </acceptance_criteria>
  <done>The scheduler can persist and resolve a postponed prompt window without discarding the accountable elapsed block.</done>
</task>

<task type="auto">
  <name>Task 2: Add delay actions to the prompt and app model</name>
  <files>Sources/TempoApp/App/TempoAppModel.swift, Sources/TempoApp/Features/CheckIn/CheckInPromptContent.swift</files>
  <read_first>Sources/TempoApp/App/TempoAppModel.swift, Sources/TempoApp/Features/CheckIn/CheckInPromptContent.swift, Sources/TempoApp/Models/AppSettingsRecord.swift, Sources/TempoApp/Scheduler/PollingScheduler.swift</read_first>
  <action>Publish delay-related prompt state from TempoAppModel, including the configured preset labels derived from settings.delayPresetMinutes. Add an app-model method named delayPrompt(byMinutes:) that calls PollingScheduler.delayCheckIn, saves scheduler state, clears the prompt search text, and dismisses the prompt without inserting a TimeEntryRecord. Update CheckInPromptContent to render delay buttons in the prompt chrome, labeled with the configured preset durations like "Delay 15 min" and "Delay 30 min", and wire them to delayPrompt(byMinutes:). Keep the project picker intact and do not add silence controls in this plan yet.</action>
  <verify>rg -n "delayPrompt|Delay .* min|delayPresetMinutes" Sources/TempoApp/App/TempoAppModel.swift Sources/TempoApp/Features/CheckIn/CheckInPromptContent.swift</verify>
  <acceptance_criteria>
    - Sources/TempoApp/App/TempoAppModel.swift contains "func delayPrompt"
    - Sources/TempoApp/Features/CheckIn/CheckInPromptContent.swift contains "Delay"
    - Sources/TempoApp/Features/CheckIn/CheckInPromptContent.swift contains "delayPresetMinutes"
    - Sources/TempoApp/App/TempoAppModel.swift does not contain "TimeEntryRecord(" within the delay method body
  </acceptance_criteria>
  <done>The prompt exposes one-click postponement actions driven by persisted settings and hides immediately when the user delays.</done>
</task>

<task type="auto">
  <name>Task 3: Add tests for delayed prompt scheduling and no-write behavior</name>
  <files>Tests/TempoAppTests/PollingSchedulerTests.swift, Tests/TempoAppTests/CheckInCompletionTests.swift, Sources/TempoApp/App/TempoAppModel.swift</files>
  <read_first>Tests/TempoAppTests/PollingSchedulerTests.swift, Tests/TempoAppTests/CheckInCompletionTests.swift, Sources/TempoApp/App/TempoAppModel.swift, Sources/TempoApp/Scheduler/PollingScheduler.swift, Sources/TempoApp/Models/SchedulerStateRecord.swift</read_first>
  <action>Extend PollingSchedulerTests with coverage proving delayCheckIn moves nextCheckInAt by the requested delay and that updateState re-enters overdue prompt state once delayedUntilAt has passed. Add app-model tests that calling delayPrompt(byMinutes:) leaves TimeEntryRecord count unchanged, clears promptSearchText, and updates schedulerStateRecord.nextCheckInAt to the delayed deadline. Keep the tests deterministic by using the existing fixed clocks and in-memory model container.</action>
  <verify>rg -n "testDelayCheckInSchedulesFuturePrompt|testDelayedPromptBecomesDueAfterDelayExpires|testDelayPromptDoesNotCreateTimeEntry" Tests/TempoAppTests/PollingSchedulerTests.swift Tests/TempoAppTests/CheckInCompletionTests.swift</verify>
  <acceptance_criteria>
    - Tests/TempoAppTests/PollingSchedulerTests.swift contains "func testDelayCheckInSchedulesFuturePrompt()"
    - Tests/TempoAppTests/PollingSchedulerTests.swift contains "func testDelayedPromptBecomesDueAfterDelayExpires()"
    - Tests/TempoAppTests/CheckInCompletionTests.swift contains "func testDelayPromptDoesNotCreateTimeEntry()"
  </acceptance_criteria>
  <done>The delay path is protected by tests that verify both scheduler timing semantics and the absence of accidental time-entry writes.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Delaying a prompt persists a concrete delayed-until timestamp in scheduler state
- [ ] Prompt delay actions come from configured delay presets in settings
- [ ] Delay actions never create a TimeEntryRecord or mark the check-in complete
- [ ] When the delay expires, scheduler refresh returns the prompt to due/overdue state using the original accountable time block
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Tempo supports postponing a due check-in and automatically re-prompts when the delay window ends
- Delays preserve tracking correctness by not logging or resetting work until the user actually classifies the prompt
  </success_criteria>

<output>
After completion, create `.planning/phases/03-deferrals-silence-and-menu-bar-control/03-01-SUMMARY.md`
</output>
