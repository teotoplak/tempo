---
phase: 03-deferrals-silence-and-menu-bar-control
plan: 03
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
  - "02-03"
files_modified:
  - Sources/TempoApp/App/TempoApp.swift
  - Sources/TempoApp/App/TempoAppModel.swift
  - Sources/TempoApp/Views/MenuBarRootView.swift
  - Sources/TempoApp/Views/AppWindowShellView.swift
  - Tests/TempoAppTests/TempoAppBootstrapTests.swift
  - Tests/TempoAppTests/PersistenceModelTests.swift
autonomous: true
requirements:
  - MENU-02
  - MENU-03
  - MENU-04
  - MENU-05
must_haves:
  truths:
    - The menu bar shows live status for the next check-in, the current project context, and total tracked time for today.
    - Quick actions let the user check in now, open analytics, open settings, unsilence when needed, and quit.
    - Menu-bar status reflects the new delay and silence scheduler states instead of only showing a static time label.
  artifacts:
    - Sources/TempoApp/Views/MenuBarRootView.swift
    - Sources/TempoApp/App/TempoAppModel.swift
    - Sources/TempoApp/App/TempoApp.swift
  key_links:
    - TempoAppModel.swift should centralize menu-bar display strings and today's total queries so the view stays thin and testable.
    - TempoApp.swift and AppWindowShellView.swift need enough navigation plumbing for quick actions to open the main window in the right section.
---

<objective>
Build the final Phase 3 menu-bar experience with status readouts and quick actions.

Purpose: Turn the menu bar from a bootstrap placeholder into the user’s primary always-on control surface for Tempo.
Output: Rich menu-bar status, today-total and current-context derivations, and quick actions wired into the existing app shell.
</objective>

<execution_context>
@/Users/teo/src/tempo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/teo/src/tempo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-check-in-logging-flow/02-03-SUMMARY.md
@.planning/phases/03-deferrals-silence-and-menu-bar-control/03-01-SUMMARY.md
@.planning/phases/03-deferrals-silence-and-menu-bar-control/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Derive menu-bar status, current context, and today's total in the app model</name>
  <files>Sources/TempoApp/App/TempoAppModel.swift, Sources/TempoApp/Models/TimeEntryRecord.swift</files>
  <read_first>Sources/TempoApp/App/TempoAppModel.swift, Sources/TempoApp/Models/TimeEntryRecord.swift, Sources/TempoApp/Models/ProjectRecord.swift, Sources/TempoApp/Scheduler/PollingScheduler.swift, Tests/TempoAppTests/PersistenceModelTests.swift</read_first>
  <action>Add app-model derived values for a countdown/status label, currentProjectContextLabel, and todaysTrackedDuration. Compute today's total from TimeEntryRecord values whose endAt falls on the current local day. Derive current project context from the most recent completed check-in project when the app is between prompts, and switch the status copy when the app is delayed or silenced so the menu bar never shows stale "Next check-in" messaging. Add a checkInNow() method that forces the prompt visible immediately without writing a time entry or resetting scheduler completion state.</action>
  <verify>rg -n "currentProjectContextLabel|todaysTrackedDuration|checkInNow|countdown" Sources/TempoApp/App/TempoAppModel.swift</verify>
  <acceptance_criteria>
    - Sources/TempoApp/App/TempoAppModel.swift contains "var currentProjectContextLabel"
    - Sources/TempoApp/App/TempoAppModel.swift contains "var todaysTrackedDuration"
    - Sources/TempoApp/App/TempoAppModel.swift contains "func checkInNow()"
    - Sources/TempoApp/App/TempoAppModel.swift contains "Calendar.current"
  </acceptance_criteria>
  <done>The app model exposes all menu-bar-facing data as derived state instead of embedding persistence logic in the view.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the placeholder menu bar with status cards and quick actions</name>
  <files>Sources/TempoApp/Views/MenuBarRootView.swift, Sources/TempoApp/App/TempoApp.swift, Sources/TempoApp/Views/AppWindowShellView.swift</files>
  <read_first>Sources/TempoApp/Views/MenuBarRootView.swift, Sources/TempoApp/App/TempoApp.swift, Sources/TempoApp/Views/AppWindowShellView.swift, Sources/TempoApp/App/TempoAppModel.swift</read_first>
  <action>Redesign MenuBarRootView to show three compact status sections: the next check-in countdown or silence/delay state, the current project context, and today's tracked total formatted with monospaced digits. Add quick actions for "Check In Now", "Open Analytics", "Projects", "Settings", and "Quit Tempo", with an additional "Unsilence" button shown only when silence is active. Update app-level wiring so the menu bar can open the main window and switch appModel.selectedWindow to .analytics or .projects before presentation, rather than always relying on the default selection.</action>
  <verify>rg -n "Check In Now|Open Analytics|current project|Today's total|selectedWindow" Sources/TempoApp/Views/MenuBarRootView.swift Sources/TempoApp/App/TempoApp.swift Sources/TempoApp/Views/AppWindowShellView.swift</verify>
  <acceptance_criteria>
    - Sources/TempoApp/Views/MenuBarRootView.swift contains "Check In Now"
    - Sources/TempoApp/Views/MenuBarRootView.swift contains "Open Analytics"
    - Sources/TempoApp/Views/MenuBarRootView.swift contains "Today's total"
    - Sources/TempoApp/Views/MenuBarRootView.swift contains "Current project"
  </acceptance_criteria>
  <done>The menu bar becomes a complete control surface with the required status readouts and navigation shortcuts.</done>
</task>

<task type="auto">
  <name>Task 3: Add deterministic tests for menu-bar derived state and actions</name>
  <files>Tests/TempoAppTests/PersistenceModelTests.swift, Tests/TempoAppTests/TempoAppBootstrapTests.swift, Sources/TempoApp/App/TempoAppModel.swift</files>
  <read_first>Tests/TempoAppTests/PersistenceModelTests.swift, Tests/TempoAppTests/TempoAppBootstrapTests.swift, Sources/TempoApp/App/TempoAppModel.swift, Sources/TempoApp/Models/TimeEntryRecord.swift, Sources/TempoApp/Models/ProjectRecord.swift</read_first>
  <action>Add model-level tests that seed time entries and verify today's total only counts entries from the current local day, currentProjectContextLabel reflects the latest completed check-in project, and checkInNow makes checkInPromptState.isPresented true immediately. Add a bootstrap or source-level assertion that the menu bar includes quick actions for analytics, settings, and quit so MENU-05 remains covered even if the layout evolves.</action>
  <verify>rg -n "testTodaysTrackedDurationOnlyCountsCurrentDay|testCurrentProjectContextUsesLatestEntry|testCheckInNowShowsPrompt|Open Analytics|Quit Tempo" Tests/TempoAppTests/PersistenceModelTests.swift Tests/TempoAppTests/TempoAppBootstrapTests.swift</verify>
  <acceptance_criteria>
    - Tests/TempoAppTests/PersistenceModelTests.swift contains "func testTodaysTrackedDurationOnlyCountsCurrentDay()"
    - Tests/TempoAppTests/PersistenceModelTests.swift contains "func testCurrentProjectContextUsesLatestEntry()"
    - Tests/TempoAppTests/TempoAppBootstrapTests.swift contains "func testCheckInNowShowsPrompt()"
    - Tests/TempoAppTests/TempoAppBootstrapTests.swift contains "Open Analytics"
    - Tests/TempoAppTests/TempoAppBootstrapTests.swift contains "Quit Tempo"
  </acceptance_criteria>
  <done>The menu-bar contract is covered through deterministic model assertions and source-level quick-action checks.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Menu bar shows a live next-check-in or silence/delay status instead of only a static clock time
- [ ] Menu bar exposes current project context derived from the latest completed check-in
- [ ] Menu bar shows today's tracked total based on persisted time entries
- [ ] Quick actions cover check in now, analytics, settings, quit, and unsilence when applicable
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Tempo’s menu bar reflects real tracking state and exposes the daily-use controls required for Phase 3
- Users can manage prompt timing and jump into analytics or settings without hunting through the main window
  </success_criteria>

<output>
After completion, create `.planning/phases/03-deferrals-silence-and-menu-bar-control/03-03-SUMMARY.md`
</output>
