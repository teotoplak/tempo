---
phase: 03-deferrals-silence-and-menu-bar-control
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
  - "02-03"
files_modified:
  - Sources/TempoApp/App/TempoAppModel.swift
  - Sources/TempoApp/Scheduler/PollingScheduler.swift
  - Sources/TempoApp/Scheduler/SchedulerStateStore.swift
  - Sources/TempoApp/Models/SchedulerStateRecord.swift
  - Sources/TempoApp/Features/CheckIn/CheckInPromptContent.swift
  - Sources/TempoApp/Views/MenuBarRootView.swift
  - Tests/TempoAppTests/PollingSchedulerTests.swift
  - Tests/TempoAppTests/TempoAppBootstrapTests.swift
autonomous: true
requirements:
  - POLL-08
  - SILN-01
  - SILN-02
  - SILN-03
  - SILN-04
must_haves:
  truths:
    - The prompt offers a silence-for-rest-of-day action that suspends prompting and tracked elapsed-time accumulation immediately.
    - Silence state automatically clears at local midnight without user intervention.
    - The menu bar exposes a manual unsilence action that restores normal scheduling from the unsilence moment.
  artifacts:
    - Sources/TempoApp/Scheduler/PollingScheduler.swift
    - Sources/TempoApp/App/TempoAppModel.swift
    - Sources/TempoApp/Views/MenuBarRootView.swift
  key_links:
    - PollingScheduler.swift must own the midnight reset and silenced snapshot semantics so app launches and wake events stay correct.
    - MenuBarRootView.swift is the primary control surface for manual unsilence outside the check-in prompt.
---

<objective>
Implement the silence mode lifecycle, including automatic midnight reset and manual unsilence.

Purpose: Give the user a way to stop polling for the rest of the current day without contaminating tracked time totals.
Output: Persisted silence state, prompt and menu-bar controls, and tests for midnight recovery and manual unsilence.
</objective>

<execution_context>
@/Users/teo/src/tempo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/teo/src/tempo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-check-in-logging-flow/02-03-SUMMARY.md
@.planning/phases/03-deferrals-silence-and-menu-bar-control/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add scheduler support for silence state and midnight reset</name>
  <files>Sources/TempoApp/Scheduler/PollingScheduler.swift, Sources/TempoApp/Scheduler/SchedulerStateStore.swift, Sources/TempoApp/Models/SchedulerStateRecord.swift</files>
  <read_first>Sources/TempoApp/Scheduler/PollingScheduler.swift, Sources/TempoApp/Scheduler/SchedulerStateStore.swift, Sources/TempoApp/Models/SchedulerStateRecord.swift, Sources/TempoApp/Models/AppSettingsRecord.swift, Tests/TempoAppTests/PollingSchedulerTests.swift</read_first>
  <action>Add persisted silence fields such as silencedAt and silenceEndsAt to SchedulerStateRecord, with silenceEndsAt set to the next local midnight when silence begins. Extend PollingSchedulerSnapshot with isSilenced so the app can hide prompt and menu-bar countdown state consistently. Add scheduler APIs named silenceUntilEndOfDay(state:settings:eventDate:) and endSilence(state:settings:eventDate:). While silence is active, updateState must return no due prompt, no carried accountable elapsed interval, and nextCheckInAt equal to silenceEndsAt. Once eventDate reaches or passes silenceEndsAt, clear silence fields automatically and schedule the next check-in from the reset moment using the normal polling interval.</action>
  <verify>rg -n "silenceEndsAt|isSilenced|silenceUntilEndOfDay|endSilence" Sources/TempoApp/Scheduler/PollingScheduler.swift Sources/TempoApp/Scheduler/SchedulerStateStore.swift Sources/TempoApp/Models/SchedulerStateRecord.swift</verify>
  <acceptance_criteria>
    - Sources/TempoApp/Models/SchedulerStateRecord.swift contains "silenceEndsAt"
    - Sources/TempoApp/Scheduler/PollingScheduler.swift contains "func silenceUntilEndOfDay"
    - Sources/TempoApp/Scheduler/PollingScheduler.swift contains "func endSilence"
    - Sources/TempoApp/Scheduler/PollingScheduler.swift contains "isSilenced"
  </acceptance_criteria>
  <done>The scheduler can enter a rest-of-day silence mode and clear it deterministically at midnight or via explicit unsilence.</done>
</task>

<task type="auto">
  <name>Task 2: Wire silence and unsilence actions through the app model and UI surfaces</name>
  <files>Sources/TempoApp/App/TempoAppModel.swift, Sources/TempoApp/Features/CheckIn/CheckInPromptContent.swift, Sources/TempoApp/Views/MenuBarRootView.swift</files>
  <read_first>Sources/TempoApp/App/TempoAppModel.swift, Sources/TempoApp/Features/CheckIn/CheckInPromptContent.swift, Sources/TempoApp/Views/MenuBarRootView.swift, Sources/TempoApp/Scheduler/PollingScheduler.swift</read_first>
  <action>Add TempoAppModel properties for isSilenced and silenceEndsAt plus methods named silenceForRestOfDay() and endSilenceMode(). SilenceForRestOfDay should clear prompt search state, hide the prompt, apply the scheduler silence result, and avoid creating any time entry or scheduler completion event. EndSilenceMode should clear silence immediately and schedule the next check-in from clock.now. Update CheckInPromptContent to add a button labeled "Silence for today". Update MenuBarRootView so when isSilenced is true it shows a prominent silence status line with the reset time and an "Unsilence" button.</action>
  <verify>rg -n "silenceForRestOfDay|endSilenceMode|Silence for today|Unsilence" Sources/TempoApp/App/TempoAppModel.swift Sources/TempoApp/Features/CheckIn/CheckInPromptContent.swift Sources/TempoApp/Views/MenuBarRootView.swift</verify>
  <acceptance_criteria>
    - Sources/TempoApp/App/TempoAppModel.swift contains "func silenceForRestOfDay"
    - Sources/TempoApp/App/TempoAppModel.swift contains "func endSilenceMode"
    - Sources/TempoApp/Features/CheckIn/CheckInPromptContent.swift contains "Silence for today"
    - Sources/TempoApp/Views/MenuBarRootView.swift contains "Unsilence"
  </acceptance_criteria>
  <done>The user can start silence from the prompt and cancel it from the menu bar without leaving the existing app surfaces.</done>
</task>

<task type="auto">
  <name>Task 3: Add tests for silence suppression, midnight reset, and manual unsilence</name>
  <files>Tests/TempoAppTests/PollingSchedulerTests.swift, Tests/TempoAppTests/TempoAppBootstrapTests.swift, Sources/TempoApp/App/TempoAppModel.swift</files>
  <read_first>Tests/TempoAppTests/PollingSchedulerTests.swift, Tests/TempoAppTests/TempoAppBootstrapTests.swift, Sources/TempoApp/App/TempoAppModel.swift, Sources/TempoApp/Scheduler/PollingScheduler.swift, Sources/TempoApp/Models/SchedulerStateRecord.swift</read_first>
  <action>Extend PollingSchedulerTests with one test proving silenceUntilEndOfDay sets silenceEndsAt to the next local midnight and suppresses prompt overdue state, one test proving updateState clears silence after midnight and schedules a fresh nextCheckInAt, and one test proving endSilence schedules from the manual unsilence time. Add an app-model bootstrap test that silenceForRestOfDay hides the prompt and zeroes the accountable elapsed interval so silenced periods do not inflate later check-ins.</action>
  <verify>rg -n "testSilenceUntilEndOfDaySuppressesPrompt|testSilenceClearsAtMidnight|testEndSilenceSchedulesFromUnsilenceTime|testSilenceForRestOfDayHidesPrompt" Tests/TempoAppTests/PollingSchedulerTests.swift Tests/TempoAppTests/TempoAppBootstrapTests.swift</verify>
  <acceptance_criteria>
    - Tests/TempoAppTests/PollingSchedulerTests.swift contains "func testSilenceUntilEndOfDaySuppressesPrompt()"
    - Tests/TempoAppTests/PollingSchedulerTests.swift contains "func testSilenceClearsAtMidnight()"
    - Tests/TempoAppTests/PollingSchedulerTests.swift contains "func testEndSilenceSchedulesFromUnsilenceTime()"
    - Tests/TempoAppTests/TempoAppBootstrapTests.swift contains "func testSilenceForRestOfDayHidesPrompt()"
  </acceptance_criteria>
  <done>Silence behavior is protected by deterministic tests around day boundaries and manual recovery.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] The prompt exposes a silence-for-today action that hides the prompt immediately
- [ ] While silenced, scheduler refresh does not accumulate or surface active tracked time
- [ ] Silence clears automatically at the next local midnight
- [ ] The menu bar exposes a manual unsilence control that resumes normal scheduling from the unsilence moment
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Tempo can suspend tracking for the rest of the day and resume correctly without corrupting elapsed-time accounting
- Silence is controllable both from the prompt and from the menu bar
  </success_criteria>

<output>
After completion, create `.planning/phases/03-deferrals-silence-and-menu-bar-control/03-02-SUMMARY.md`
</output>
