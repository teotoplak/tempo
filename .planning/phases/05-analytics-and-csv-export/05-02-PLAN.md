---
phase: 05-analytics-and-csv-export
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - Sources/TempoApp/Views/AppWindowShellView.swift
  - Sources/TempoApp/App/TempoAppModel.swift
  - Sources/TempoApp/Features/Analytics/AnalyticsView.swift
  - Sources/TempoApp/Features/Analytics/AnalyticsChartSection.swift
  - Tests/TempoAppTests/AnalyticsPresentationTests.swift
  - Tests/TempoAppTests/TempoAppBootstrapTests.swift
autonomous: true
requirements:
  - ANLY-01
  - ANLY-02
  - ANLY-03
  - ANLY-04
  - ANLY-05
  - ANLY-06
must_haves:
  truths:
    - The main Tempo window can open a real analytics screen instead of the placeholder text and switch among daily, weekly, monthly, and yearly summaries.
    - The analytics screen presents total duration, project percentages, and charted allocation from the exact snapshot produced by the aggregation layer.
    - The visual structure follows the Daily Time Tracking-style workflow by leading with period controls, summary metrics, and project allocation at a glance.
  artifacts:
    - Sources/TempoApp/Features/Analytics/AnalyticsView.swift
    - Sources/TempoApp/Features/Analytics/AnalyticsChartSection.swift
    - Sources/TempoApp/Views/AppWindowShellView.swift
    - Tests/TempoAppTests/AnalyticsPresentationTests.swift
  key_links:
    - AnalyticsView.swift must bind to TempoAppModel analytics state so totals, period labels, and charts all stay synchronized.
    - AppWindowShellView.swift must replace the placeholder with the real AnalyticsView so menu-bar "Open Analytics" reaches the finished feature.
---

<objective>
Implement charts and analytics presentation.

Purpose: Turn the analytics snapshot into a usable review screen that helps the user inspect tracked time by period and project.
Output: A real analytics screen, period picker, summary cards, project breakdown list, chart section, and UI-focused tests.
</objective>

<execution_context>
@/Users/teo/src/tempo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/teo/src/tempo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-analytics-and-csv-export/05-01-PLAN.md
@Sources/TempoApp/Views/AppWindowShellView.swift
@Sources/TempoApp/App/TempoAppModel.swift
@Sources/TempoApp/Views/MenuBarRootView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the analytics placeholder with a real analytics screen</name>
  <files>Sources/TempoApp/Views/AppWindowShellView.swift, Sources/TempoApp/Features/Analytics/AnalyticsView.swift, Sources/TempoApp/App/TempoAppModel.swift</files>
  <read_first>Sources/TempoApp/Views/AppWindowShellView.swift, Sources/TempoApp/App/TempoAppModel.swift, Sources/TempoApp/Views/MenuBarRootView.swift, Sources/TempoApp/Features/Analytics/AnalyticsModels.swift</read_first>
  <action>Create Sources/TempoApp/Features/Analytics/AnalyticsView.swift and replace the analyticsPlaceholder branch in AppWindowShellView with AnalyticsView(appModel: appModel). AnalyticsView must render a title "Analytics", a segmented Picker bound to appModel.selectedAnalyticsRange with labels "Daily", "Weekly", "Monthly", and "Yearly", a period label using appModel.analyticsPeriod.label, and summary cards titled "Total tracked" and "Top project". In TempoAppModel ensure selectAnalyticsRange(_:) refreshes the snapshot immediately and expose formatted helpers named analyticsTotalDurationText and analyticsTopProjectSummaryText so the view does not duplicate formatting logic.</action>
  <verify>rg -n "AnalyticsView\\(appModel: appModel\\)|Text\\(\"Analytics\"\\)|\"Daily\"|\"Weekly\"|\"Monthly\"|\"Yearly\"|analyticsTotalDurationText|analyticsTopProjectSummaryText" Sources/TempoApp/Views/AppWindowShellView.swift Sources/TempoApp/Features/Analytics/AnalyticsView.swift Sources/TempoApp/App/TempoAppModel.swift</verify>
  <acceptance_criteria>
    - Sources/TempoApp/Views/AppWindowShellView.swift contains "AnalyticsView(appModel: appModel)"
    - Sources/TempoApp/Features/Analytics/AnalyticsView.swift contains "Text(\"Analytics\")"
    - Sources/TempoApp/Features/Analytics/AnalyticsView.swift contains "\"Daily\""
    - Sources/TempoApp/Features/Analytics/AnalyticsView.swift contains "\"Weekly\""
    - Sources/TempoApp/Features/Analytics/AnalyticsView.swift contains "\"Monthly\""
    - Sources/TempoApp/Features/Analytics/AnalyticsView.swift contains "\"Yearly\""
    - Sources/TempoApp/App/TempoAppModel.swift contains "analyticsTotalDurationText"
  </acceptance_criteria>
  <done>The main window now opens a concrete analytics screen with live period switching and top-level summary metrics.</done>
</task>

<task type="auto">
  <name>Task 2: Render project allocation chart and percentage breakdown</name>
  <files>Sources/TempoApp/Features/Analytics/AnalyticsView.swift, Sources/TempoApp/Features/Analytics/AnalyticsChartSection.swift</files>
  <read_first>Sources/TempoApp/Features/Analytics/AnalyticsView.swift, Sources/TempoApp/Features/Analytics/AnalyticsModels.swift, Sources/TempoApp/App/TempoAppModel.swift, Package.swift</read_first>
  <action>Create Sources/TempoApp/Features/Analytics/AnalyticsChartSection.swift that imports Charts and renders a donut chart from appModel.analyticsProjectSummaries using each summary's projectName and totalDuration. In AnalyticsView add a section header "Project allocation", include AnalyticsChartSection, and render a project list where each row shows the project name, a formatted duration string, percentage text using PercentFormatStyle, and entry count. Add an empty state with the exact copy "No tracked time in this period" when analyticsProjectSummaries is empty. Keep the layout in a review-first order: period selector, totals, chart, then breakdown table.</action>
  <verify>rg -n "import Charts|Project allocation|No tracked time in this period|PercentFormatStyle|entryCount|analyticsProjectSummaries" Sources/TempoApp/Features/Analytics/AnalyticsView.swift Sources/TempoApp/Features/Analytics/AnalyticsChartSection.swift</verify>
  <acceptance_criteria>
    - Sources/TempoApp/Features/Analytics/AnalyticsChartSection.swift contains "import Charts"
    - Sources/TempoApp/Features/Analytics/AnalyticsView.swift contains "Project allocation"
    - Sources/TempoApp/Features/Analytics/AnalyticsView.swift contains "No tracked time in this period"
    - Sources/TempoApp/Features/Analytics/AnalyticsView.swift contains "PercentFormatStyle"
    - Sources/TempoApp/Features/Analytics/AnalyticsView.swift contains "entryCount"
  </acceptance_criteria>
  <done>The analytics screen shows charted time allocation plus a readable project-by-project percentage breakdown for the selected period.</done>
</task>

<task type="auto">
  <name>Task 3: Add presentation tests that lock in analytics entry points and copy</name>
  <files>Tests/TempoAppTests/AnalyticsPresentationTests.swift, Tests/TempoAppTests/TempoAppBootstrapTests.swift, Sources/TempoApp/Features/Analytics/AnalyticsView.swift, Sources/TempoApp/Views/AppWindowShellView.swift</files>
  <read_first>Tests/TempoAppTests/TempoAppBootstrapTests.swift, Sources/TempoApp/Features/Analytics/AnalyticsView.swift, Sources/TempoApp/Views/AppWindowShellView.swift, Sources/TempoApp/App/TempoAppModel.swift</read_first>
  <action>Create Tests/TempoAppTests/AnalyticsPresentationTests.swift with source-level tests named testAppWindowShellUsesAnalyticsView(), testAnalyticsViewContainsPeriodPickerLabels(), testAnalyticsViewContainsProjectAllocationSection(), and testAnalyticsViewDefinesEmptyStateCopy(). Read the source files as strings, then assert the analytics shell contains AnalyticsView(appModel: appModel), the picker labels "Daily", "Weekly", "Monthly", and "Yearly" are present, the section title "Project allocation" exists, and the empty-state copy is unchanged. Extend TempoAppBootstrapTests with a model-level test named testAnalyticsWindowSectionStillExists() that verifies TempoAppModel.WindowSection.allCases contains .analytics.</action>
  <verify>rg -n "testAppWindowShellUsesAnalyticsView|testAnalyticsViewContainsPeriodPickerLabels|testAnalyticsViewContainsProjectAllocationSection|testAnalyticsViewDefinesEmptyStateCopy|testAnalyticsWindowSectionStillExists" Tests/TempoAppTests/AnalyticsPresentationTests.swift Tests/TempoAppTests/TempoAppBootstrapTests.swift</verify>
  <acceptance_criteria>
    - Tests/TempoAppTests/AnalyticsPresentationTests.swift contains "func testAppWindowShellUsesAnalyticsView()"
    - Tests/TempoAppTests/AnalyticsPresentationTests.swift contains "func testAnalyticsViewContainsPeriodPickerLabels()"
    - Tests/TempoAppTests/AnalyticsPresentationTests.swift contains "func testAnalyticsViewContainsProjectAllocationSection()"
    - Tests/TempoAppTests/AnalyticsPresentationTests.swift contains "func testAnalyticsViewDefinesEmptyStateCopy()"
    - Tests/TempoAppTests/TempoAppBootstrapTests.swift contains "func testAnalyticsWindowSectionStillExists()"
  </acceptance_criteria>
  <done>The analytics UI entry point and critical copy are protected by tests so later changes do not silently regress the reporting workflow.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `swift test --filter AnalyticsPresentationTests` passes
- [ ] `swift test --filter TempoAppBootstrapTests/testAnalyticsWindowSectionStillExists` passes
- [ ] The main window analytics branch uses `AnalyticsView(appModel: appModel)` and no longer relies on placeholder-only copy
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Tempo exposes a genuine analytics screen with period switching, totals, percentages, and a chart
- The analytics presentation matches the intended review workflow instead of burying data in the menu bar
  </success_criteria>

<output>
After completion, create `.planning/phases/05-analytics-and-csv-export/05-02-SUMMARY.md`
</output>
