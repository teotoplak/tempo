---
phase: 05-analytics-and-csv-export
plan: 03
type: execute
wave: 3
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - Sources/TempoApp/App/TempoAppModel.swift
  - Sources/TempoApp/Features/Analytics/AnalyticsView.swift
  - Sources/TempoApp/Features/Analytics/CSVExportService.swift
  - Tests/TempoAppTests/CSVExportTests.swift
  - Tests/TempoAppTests/AnalyticsPresentationTests.swift
autonomous: true
requirements:
  - EXPT-01
  - EXPT-02
must_haves:
  truths:
    - Tempo can export tracked entries from the selected analytics period to a UTF-8 CSV file without any network dependency.
    - Exported rows include date, start time, end time, duration, and project name for each included TimeEntryRecord in the selected period.
    - The analytics screen exposes a clear export action so reporting and portable data leave the app from the same workflow.
  artifacts:
    - Sources/TempoApp/Features/Analytics/CSVExportService.swift
    - Sources/TempoApp/Features/Analytics/AnalyticsView.swift
    - Sources/TempoApp/App/TempoAppModel.swift
    - Tests/TempoAppTests/CSVExportTests.swift
  key_links:
    - CSVExportService.swift must read the same analytics period boundaries as AnalyticsStore.swift so the exported rows match the visible report.
    - AnalyticsView.swift must call TempoAppModel export APIs instead of writing files directly so save-panel behavior and errors stay centralized.
---

<objective>
Implement CSV export from tracked entries.

Purpose: Let the user take Tempo’s local tracking data outside the app without retyping or manual cleanup.
Output: CSV export service, analytics-screen export action, save-panel flow, and tests for file contents and UI hook-up.
</objective>

<execution_context>
@/Users/teo/src/tempo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/teo/src/tempo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-analytics-and-csv-export/05-01-PLAN.md
@.planning/phases/05-analytics-and-csv-export/05-02-PLAN.md
@Sources/TempoApp/App/TempoAppModel.swift
@Sources/TempoApp/Models/TimeEntryRecord.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create a CSV export service for analytics periods</name>
  <files>Sources/TempoApp/Features/Analytics/CSVExportService.swift, Sources/TempoApp/App/TempoAppModel.swift</files>
  <read_first>Sources/TempoApp/Models/TimeEntryRecord.swift, Sources/TempoApp/Features/Analytics/AnalyticsStore.swift, Sources/TempoApp/App/TempoAppModel.swift, Tests/TempoAppTests/PersistenceModelTests.swift</read_first>
  <action>Create Sources/TempoApp/Features/Analytics/CSVExportService.swift with a @MainActor type named CSVExportService that accepts ModelContext and Calendar. Implement methods exportRows(range:referenceDate:) and csvString(range:referenceDate:) using the same AnalyticsStore.period(for:referenceDate:calendar:) logic so only entries inside the selected period are exported. The CSV header must be exactly "date,start_time,end_time,duration_minutes,project_name". Each row must format date as yyyy-MM-dd, start and end times as HH:mm, duration as whole minutes, and project_name as the linked project name or "Unassigned" when the project is nil. Sort rows by startAt ascending before writing.</action>
  <verify>rg -n "final class CSVExportService|func exportRows\\(range:|func csvString\\(range:|date,start_time,end_time,duration_minutes,project_name|yyyy-MM-dd|HH:mm|Unassigned" Sources/TempoApp/Features/Analytics/CSVExportService.swift</verify>
  <acceptance_criteria>
    - Sources/TempoApp/Features/Analytics/CSVExportService.swift contains "final class CSVExportService"
    - Sources/TempoApp/Features/Analytics/CSVExportService.swift contains "func exportRows(range: AnalyticsRange"
    - Sources/TempoApp/Features/Analytics/CSVExportService.swift contains "func csvString(range: AnalyticsRange"
    - Sources/TempoApp/Features/Analytics/CSVExportService.swift contains "date,start_time,end_time,duration_minutes,project_name"
    - Sources/TempoApp/Features/Analytics/CSVExportService.swift contains "\"Unassigned\""
  </acceptance_criteria>
  <done>Tempo has a deterministic service that can convert the selected analytics period into stable CSV rows and text.</done>
</task>

<task type="auto">
  <name>Task 2: Wire analytics export through TempoAppModel and the analytics screen</name>
  <files>Sources/TempoApp/App/TempoAppModel.swift, Sources/TempoApp/Features/Analytics/AnalyticsView.swift</files>
  <read_first>Sources/TempoApp/App/TempoAppModel.swift, Sources/TempoApp/Features/Analytics/AnalyticsView.swift, Sources/TempoApp/Features/Analytics/CSVExportService.swift, Sources/TempoApp/App/TempoApp.swift</read_first>
  <action>In TempoAppModel add a csvExportService property plus observable export state named analyticsExportStatusMessage and analyticsExportErrorMessage. Implement exportAnalyticsCSV() so it opens an NSSavePanel with nameFieldStringValue "tempo-analytics.csv", allows only the csv filename extension, writes the UTF-8 string returned by CSVExportService.csvString(range:referenceDate:), and updates analyticsExportStatusMessage with "CSV exported" on success or analyticsExportErrorMessage on failure. In AnalyticsView add a button titled "Export CSV" near the period controls, disable it only when both analyticsProjectSummaries is empty and analyticsTotalDuration is 0, and render the status or error message below the button using the exact labels "CSV exported" and "Export failed".</action>
  <verify>rg -n "func exportAnalyticsCSV\\(|NSSavePanel|tempo-analytics.csv|Export CSV|CSV exported|Export failed|analyticsExportStatusMessage|analyticsExportErrorMessage" Sources/TempoApp/App/TempoAppModel.swift Sources/TempoApp/Features/Analytics/AnalyticsView.swift</verify>
  <acceptance_criteria>
    - Sources/TempoApp/App/TempoAppModel.swift contains "func exportAnalyticsCSV()"
    - Sources/TempoApp/App/TempoAppModel.swift contains "NSSavePanel"
    - Sources/TempoApp/App/TempoAppModel.swift contains "tempo-analytics.csv"
    - Sources/TempoApp/Features/Analytics/AnalyticsView.swift contains "Export CSV"
    - Sources/TempoApp/Features/Analytics/AnalyticsView.swift contains "CSV exported"
    - Sources/TempoApp/Features/Analytics/AnalyticsView.swift contains "Export failed"
  </acceptance_criteria>
  <done>The analytics workflow includes a concrete CSV export action that writes a file through a native macOS save panel.</done>
</task>

<task type="auto">
  <name>Task 3: Add tests for CSV content and export UI wiring</name>
  <files>Tests/TempoAppTests/CSVExportTests.swift, Tests/TempoAppTests/AnalyticsPresentationTests.swift, Sources/TempoApp/Features/Analytics/CSVExportService.swift</files>
  <read_first>Tests/TempoAppTests/AnalyticsAggregationTests.swift, Tests/TempoAppTests/AnalyticsPresentationTests.swift, Sources/TempoApp/Features/Analytics/CSVExportService.swift, Sources/TempoApp/Features/Analytics/AnalyticsView.swift</read_first>
  <action>Create Tests/TempoAppTests/CSVExportTests.swift with tests named testCSVStringIncludesRequiredColumns(), testCSVStringFormatsRowsInStartTimeOrder(), and testCSVStringUsesSelectedAnalyticsPeriodBoundaries(). Seed in-memory TimeEntryRecord rows and assert the exported string contains the header "date,start_time,end_time,duration_minutes,project_name", includes expected formatted dates and HH:mm timestamps, excludes rows outside the requested period, and orders rows by startAt ascending. Extend AnalyticsPresentationTests with a source-level test named testAnalyticsViewContainsExportCSVAction() that asserts the view source includes "Export CSV", "CSV exported", and "Export failed".</action>
  <verify>rg -n "testCSVStringIncludesRequiredColumns|testCSVStringFormatsRowsInStartTimeOrder|testCSVStringUsesSelectedAnalyticsPeriodBoundaries|testAnalyticsViewContainsExportCSVAction" Tests/TempoAppTests/CSVExportTests.swift Tests/TempoAppTests/AnalyticsPresentationTests.swift</verify>
  <acceptance_criteria>
    - Tests/TempoAppTests/CSVExportTests.swift contains "func testCSVStringIncludesRequiredColumns()"
    - Tests/TempoAppTests/CSVExportTests.swift contains "func testCSVStringFormatsRowsInStartTimeOrder()"
    - Tests/TempoAppTests/CSVExportTests.swift contains "func testCSVStringUsesSelectedAnalyticsPeriodBoundaries()"
    - Tests/TempoAppTests/AnalyticsPresentationTests.swift contains "func testAnalyticsViewContainsExportCSVAction()"
  </acceptance_criteria>
  <done>The CSV export path is guarded by tests that prove the file columns, ordering, and analytics-screen action stay intact.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `swift test --filter CSVExportTests` passes
- [ ] `rg -n "date,start_time,end_time,duration_minutes,project_name|Export CSV|NSSavePanel" Sources/TempoApp/Features/Analytics/CSVExportService.swift Sources/TempoApp/Features/Analytics/AnalyticsView.swift Sources/TempoApp/App/TempoAppModel.swift` finds the export contract
- [ ] Exported CSV rows use the selected analytics period and include date, start time, end time, duration, and project name for each entry
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Tempo can export selected-period time entries to a CSV file with the required columns
- The export action lives on the analytics screen and uses the same period boundaries the user is inspecting
  </success_criteria>

<output>
After completion, create `.planning/phases/05-analytics-and-csv-export/05-03-SUMMARY.md`
</output>
