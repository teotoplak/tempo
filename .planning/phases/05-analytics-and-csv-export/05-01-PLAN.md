---
phase: 05-analytics-and-csv-export
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Sources/TempoApp/App/TempoAppModel.swift
  - Sources/TempoApp/Features/Analytics/AnalyticsModels.swift
  - Sources/TempoApp/Features/Analytics/AnalyticsStore.swift
  - Tests/TempoAppTests/AnalyticsAggregationTests.swift
autonomous: true
requirements:
  - ANLY-02
  - ANLY-04
  - ANLY-05
must_haves:
  truths:
    - Tempo can derive analytics data for day, week, month, and year periods from local TimeEntryRecord rows without network access.
    - Each analytics period produces a total tracked duration plus per-project totals and percentages whose sum matches the selected period total.
    - Analytics state lives in TempoAppModel so later views and export actions can use the same period boundaries and summaries.
  artifacts:
    - Sources/TempoApp/Features/Analytics/AnalyticsModels.swift
    - Sources/TempoApp/Features/Analytics/AnalyticsStore.swift
    - Sources/TempoApp/App/TempoAppModel.swift
    - Tests/TempoAppTests/AnalyticsAggregationTests.swift
  key_links:
    - AnalyticsStore.swift must remain the single source of truth for period-boundary filtering and per-project aggregation so charts, totals, and CSV export do not diverge.
    - TempoAppModel.swift must expose selected analytics period state and refreshed summaries instead of letting views query SwiftData directly.
---

<objective>
Build aggregation queries for analytics periods and project totals.

Purpose: Give the app a deterministic reporting layer that can summarize tracked time the same way everywhere Phase 5 needs it.
Output: Analytics period models, a SwiftData-backed analytics store, app-model refresh hooks, and deterministic aggregation tests.
</objective>

<execution_context>
@/Users/teo/src/tempo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/teo/src/tempo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Sources/TempoApp/App/TempoAppModel.swift
@Sources/TempoApp/Models/TimeEntryRecord.swift
@Tests/TempoAppTests/PersistenceModelTests.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define analytics range and summary models</name>
  <files>Sources/TempoApp/Features/Analytics/AnalyticsModels.swift, Sources/TempoApp/App/TempoAppModel.swift</files>
  <read_first>Sources/TempoApp/App/TempoAppModel.swift, Sources/TempoApp/Models/TimeEntryRecord.swift, Sources/TempoApp/Views/AppWindowShellView.swift</read_first>
  <action>Create Sources/TempoApp/Features/Analytics/AnalyticsModels.swift with public app-target types named AnalyticsRange, AnalyticsPeriod, AnalyticsProjectSummary, and AnalyticsSummarySnapshot. AnalyticsRange must be a CaseIterable enum with cases day, week, month, and year plus a title string for "Daily", "Weekly", "Monthly", and "Yearly". AnalyticsPeriod must hold startDate, endDate, and label. AnalyticsProjectSummary must hold projectID, projectName, totalDuration, percentageOfTotal, and entryCount. AnalyticsSummarySnapshot must hold period, totalDuration, projectSummaries, and topProjectName. In TempoAppModel add observable properties selectedAnalyticsRange, analyticsPeriod, analyticsTotalDuration, analyticsProjectSummaries, and analyticsTopProjectName with defaults based on clock.now so later plans can bind directly to stable analytics state.</action>
  <verify>rg -n "enum AnalyticsRange|case day|case week|case month|case year|struct AnalyticsSummarySnapshot|analyticsProjectSummaries|analyticsTopProjectName" Sources/TempoApp/Features/Analytics/AnalyticsModels.swift Sources/TempoApp/App/TempoAppModel.swift</verify>
  <acceptance_criteria>
    - Sources/TempoApp/Features/Analytics/AnalyticsModels.swift contains "enum AnalyticsRange: String, CaseIterable"
    - Sources/TempoApp/Features/Analytics/AnalyticsModels.swift contains "case day"
    - Sources/TempoApp/Features/Analytics/AnalyticsModels.swift contains "case week"
    - Sources/TempoApp/Features/Analytics/AnalyticsModels.swift contains "case month"
    - Sources/TempoApp/Features/Analytics/AnalyticsModels.swift contains "case year"
    - Sources/TempoApp/Features/Analytics/AnalyticsModels.swift contains "struct AnalyticsSummarySnapshot"
    - Sources/TempoApp/App/TempoAppModel.swift contains "var selectedAnalyticsRange"
    - Sources/TempoApp/App/TempoAppModel.swift contains "var analyticsProjectSummaries"
  </acceptance_criteria>
  <done>The app model has concrete analytics state and the new feature types define exactly how periods, totals, and per-project percentages are represented.</done>
</task>

<task type="auto">
  <name>Task 2: Implement SwiftData-backed aggregation queries</name>
  <files>Sources/TempoApp/Features/Analytics/AnalyticsStore.swift, Sources/TempoApp/App/TempoAppModel.swift</files>
  <read_first>Sources/TempoApp/Models/TimeEntryRecord.swift, Sources/TempoApp/App/TempoAppModel.swift, Sources/TempoApp/Persistence/TempoModelContainer.swift, Tests/TempoAppTests/CheckInCompletionTests.swift</read_first>
  <action>Create Sources/TempoApp/Features/Analytics/AnalyticsStore.swift with a @MainActor type named AnalyticsStore that accepts a ModelContext. Implement methods period(for:referenceDate:calendar:) and summary(range:referenceDate:calendar:) so day/week/month/year windows are derived from Calendar.current and fetch TimeEntryRecord rows whose endAt is greater than or equal to startDate and less than endDate. Build AnalyticsProjectSummary rows by grouping on project.id, using "Unassigned" when project is nil, sorting descending by totalDuration then ascending by projectName, and computing percentageOfTotal as totalDuration / totalDurationForPeriod with 0 when the total is zero. In TempoAppModel create an analyticsStore property, call refreshAnalytics(referenceDate:) from init and performInitialLaunchIfNeeded(), and call refreshAnalytics(referenceDate: clock.now) immediately after successful writes in selectProjectForPrompt(_:), assignPendingIdle(to:), discardPendingIdle(), splitPendingIdle(firstProject:firstDurationMinutes:secondProject:), and renameProject(_:to:). Add methods refreshAnalytics(referenceDate:) and selectAnalyticsRange(_:) that update analyticsPeriod, analyticsTotalDuration, analyticsProjectSummaries, and analyticsTopProjectName from AnalyticsStore.summary.</action>
  <verify>rg -n "final class AnalyticsStore|func period\\(for:|func summary\\(range:|refreshAnalytics\\(referenceDate:|selectAnalyticsRange\\(|percentageOfTotal|Unassigned" Sources/TempoApp/Features/Analytics/AnalyticsStore.swift Sources/TempoApp/App/TempoAppModel.swift</verify>
  <acceptance_criteria>
    - Sources/TempoApp/Features/Analytics/AnalyticsStore.swift contains "final class AnalyticsStore"
    - Sources/TempoApp/Features/Analytics/AnalyticsStore.swift contains "func period(for range: AnalyticsRange"
    - Sources/TempoApp/Features/Analytics/AnalyticsStore.swift contains "func summary(range: AnalyticsRange"
    - Sources/TempoApp/Features/Analytics/AnalyticsStore.swift contains "\"Unassigned\""
    - Sources/TempoApp/App/TempoAppModel.swift contains "func refreshAnalytics(referenceDate:"
    - Sources/TempoApp/App/TempoAppModel.swift contains "func selectAnalyticsRange(_ range: AnalyticsRange)"
    - Sources/TempoApp/App/TempoAppModel.swift contains "analyticsTotalDuration"
  </acceptance_criteria>
  <done>Tempo can recompute day, week, month, and year analytics snapshots from local entries and expose them through TempoAppModel.</done>
</task>

<task type="auto">
  <name>Task 3: Add deterministic aggregation tests for totals and percentages</name>
  <files>Tests/TempoAppTests/AnalyticsAggregationTests.swift, Sources/TempoApp/Features/Analytics/AnalyticsStore.swift, Sources/TempoApp/App/TempoAppModel.swift</files>
  <read_first>Tests/TempoAppTests/PersistenceModelTests.swift, Tests/TempoAppTests/CheckInCompletionTests.swift, Sources/TempoApp/Features/Analytics/AnalyticsStore.swift, Sources/TempoApp/App/TempoAppModel.swift</read_first>
  <action>Create Tests/TempoAppTests/AnalyticsAggregationTests.swift. Add tests named testDailySummaryGroupsDurationsByProject(), testWeeklySummaryFiltersEntriesOutsidePeriod(), testMonthlySummaryComputesPercentagesFromProjectTotals(), and testSelectingAnalyticsRangeRefreshesAppModelSnapshot(). Seed an in-memory TempoAppModel with fixed dates and multiple TimeEntryRecord rows across different projects and dates. Assert that the day/week/month/year summaries use the expected startDate and endDate boundaries, the totalDuration equals the sum of included entries, projectSummaries are ordered by descending totalDuration, and the percentageOfTotal values equal 0.5, 0.3333, or other exact seeded ratios within a small XCTAssertEqual accuracy tolerance.</action>
  <verify>rg -n "testDailySummaryGroupsDurationsByProject|testWeeklySummaryFiltersEntriesOutsidePeriod|testMonthlySummaryComputesPercentagesFromProjectTotals|testSelectingAnalyticsRangeRefreshesAppModelSnapshot" Tests/TempoAppTests/AnalyticsAggregationTests.swift</verify>
  <acceptance_criteria>
    - Tests/TempoAppTests/AnalyticsAggregationTests.swift contains "func testDailySummaryGroupsDurationsByProject()"
    - Tests/TempoAppTests/AnalyticsAggregationTests.swift contains "func testWeeklySummaryFiltersEntriesOutsidePeriod()"
    - Tests/TempoAppTests/AnalyticsAggregationTests.swift contains "func testMonthlySummaryComputesPercentagesFromProjectTotals()"
    - Tests/TempoAppTests/AnalyticsAggregationTests.swift contains "func testSelectingAnalyticsRangeRefreshesAppModelSnapshot()"
  </acceptance_criteria>
  <done>The aggregation layer is covered by deterministic tests that prove the selected period totals and percentages are mathematically correct.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `swift test --filter AnalyticsAggregationTests` passes
- [ ] `rg -n "AnalyticsRange|AnalyticsStore|refreshAnalytics|analyticsProjectSummaries" Sources/TempoApp/App/TempoAppModel.swift Sources/TempoApp/Features/Analytics/AnalyticsModels.swift Sources/TempoApp/Features/Analytics/AnalyticsStore.swift` finds the new analytics domain
- [ ] Day, week, month, and year summaries each produce a total plus per-project percentages from the same data source
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Tempo can compute local analytics totals and project percentages for daily, weekly, monthly, and yearly views
- Later UI and export work can consume one shared analytics snapshot instead of duplicating period math
  </success_criteria>

<output>
After completion, create `.planning/phases/05-analytics-and-csv-export/05-01-SUMMARY.md`
</output>
