---
phase: 06-launch-and-correctness-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Sources/TempoApp/App/TempoAppModel.swift
  - Sources/TempoApp/Models/AppSettingsRecord.swift
  - Sources/TempoApp/Features/Settings/SettingsPopoverView.swift
  - Sources/TempoApp/Support/LaunchAtLoginController.swift
  - Tests/TempoAppTests/LaunchAtLoginTests.swift
  - Tests/TempoAppTests/TempoAppBootstrapTests.swift
autonomous: true
requirements:
  - SETG-04
must_haves:
  truths:
    - Tempo exposes a real launch-at-login preference that the user can toggle from settings without editing system configuration manually.
    - The app-model launch-at-login state is backed by a concrete macOS login-item integration and remains synchronized with persisted settings.
    - Failures to register or unregister the login item surface clearly and do not leave the local settings record lying about the active state.
  artifacts:
    - Sources/TempoApp/Support/LaunchAtLoginController.swift
    - Sources/TempoApp/App/TempoAppModel.swift
    - Sources/TempoApp/Models/AppSettingsRecord.swift
    - Sources/TempoApp/Features/Settings/SettingsPopoverView.swift
    - Tests/TempoAppTests/LaunchAtLoginTests.swift
  key_links:
    - LaunchAtLoginController.swift must isolate ServiceManagement usage so the rest of the app can be tested with a fake controller instead of touching the real login-item registration API.
    - TempoAppModel.swift must be the single place that synchronizes the ServiceManagement state with AppSettingsRecord so the settings view stays declarative and persistence does not drift.
---

<objective>
Implement launch-at-login integration and settings wiring.

Purpose: Finish the only explicit Phase 6 requirement by giving the existing settings surface a real launch-at-login toggle backed by the native macOS registration API.
Output: A ServiceManagement wrapper, persisted launch-at-login preference, settings toggle with error handling, and deterministic tests.
</objective>

<execution_context>
@/Users/teo/src/tempo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/teo/src/tempo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Package.swift
@Sources/TempoApp/App/TempoApp.swift
@Sources/TempoApp/App/TempoAppModel.swift
@Sources/TempoApp/Models/AppSettingsRecord.swift
@Sources/TempoApp/Features/Settings/SettingsPopoverView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a launch-at-login controller abstraction around ServiceManagement</name>
  <files>Sources/TempoApp/Support/LaunchAtLoginController.swift, Sources/TempoApp/App/TempoAppModel.swift</files>
  <read_first>Sources/TempoApp/Support/LaunchAtLoginController.swift, Package.swift, Sources/TempoApp/App/TempoApp.swift, Sources/TempoApp/App/TempoAppModel.swift, Tests/TempoAppTests/TempoAppBootstrapTests.swift</read_first>
  <action>Create Sources/TempoApp/Support/LaunchAtLoginController.swift. Define a @MainActor protocol named LaunchAtLoginControlling with `var isEnabled: Bool { get }` and `func setEnabled(_ enabled: Bool) throws`. Add a concrete type named SMAppServiceLaunchAtLoginController that imports ServiceManagement, reads `SMAppService.mainApp.status == .enabled`, calls `register()` when enabling, and `unregister()` when disabling. Add a LocalizedError enum named LaunchAtLoginControllerError with cases `registrationFailed` and `unregistrationFailed` that wrap the underlying ServiceManagement failure. In TempoAppModel add an injected property `launchAtLoginController`, observable properties `launchAtLoginEnabled` and `launchAtLoginErrorMessage`, and initialize `launchAtLoginEnabled` from `launchAtLoginController.isEnabled` before the first settings render.</action>
  <verify>rg -n "protocol LaunchAtLoginControlling|SMAppServiceLaunchAtLoginController|import ServiceManagement|LaunchAtLoginControllerError|launchAtLoginController|launchAtLoginEnabled|launchAtLoginErrorMessage" Sources/TempoApp/Support/LaunchAtLoginController.swift Sources/TempoApp/App/TempoAppModel.swift</verify>
  <acceptance_criteria>
    - Sources/TempoApp/Support/LaunchAtLoginController.swift contains "protocol LaunchAtLoginControlling"
    - Sources/TempoApp/Support/LaunchAtLoginController.swift contains "struct SMAppServiceLaunchAtLoginController"
    - Sources/TempoApp/Support/LaunchAtLoginController.swift contains "import ServiceManagement"
    - Sources/TempoApp/Support/LaunchAtLoginController.swift contains "SMAppService.mainApp"
    - Sources/TempoApp/App/TempoAppModel.swift contains "var launchAtLoginEnabled"
    - Sources/TempoApp/App/TempoAppModel.swift contains "var launchAtLoginErrorMessage"
  </acceptance_criteria>
  <done>Tempo has a testable boundary around the login-item API and concrete app-model state for launch-at-login.</done>
</task>

<task type="auto">
  <name>Task 2: Persist and expose the launch-at-login preference in the settings UI</name>
  <files>Sources/TempoApp/Models/AppSettingsRecord.swift, Sources/TempoApp/App/TempoAppModel.swift, Sources/TempoApp/Features/Settings/SettingsPopoverView.swift</files>
  <read_first>Sources/TempoApp/Models/AppSettingsRecord.swift, Sources/TempoApp/App/TempoAppModel.swift, Sources/TempoApp/Features/Settings/SettingsPopoverView.swift, Sources/TempoApp/Views/MenuBarRootView.swift</read_first>
  <action>Extend AppSettingsRecord with `var launchAtLoginEnabled: Bool` defaulting to false. In TempoAppModel add methods `syncLaunchAtLoginPreferenceFromSystem()` and `saveLaunchAtLoginPreference(_ enabled: Bool) throws`. `syncLaunchAtLoginPreferenceFromSystem()` must read `launchAtLoginController.isEnabled`, assign that value to both `launchAtLoginEnabled` and `settings.launchAtLoginEnabled`, and save the model context so persisted settings match the real system state on bootstrap. `saveLaunchAtLoginPreference(_:)` must call `launchAtLoginController.setEnabled(enabled)`, then update `launchAtLoginEnabled`, `settings.launchAtLoginEnabled`, clear `launchAtLoginErrorMessage`, and save the model context; on failure it must leave `launchAtLoginEnabled` reverted to `launchAtLoginController.isEnabled` and set `launchAtLoginErrorMessage` to the thrown localizedDescription. In SettingsPopoverView add a `Section("Launch at Login")` containing a `Toggle("Launch Tempo when I sign in", isOn: ...)`, caption text "Uses the native macOS login item registration for this app.", and an error Text bound to `appModel.launchAtLoginErrorMessage`.</action>
  <verify>rg -n "launchAtLoginEnabled|syncLaunchAtLoginPreferenceFromSystem|saveLaunchAtLoginPreference|Section\\(\"Launch at Login\"\\)|Toggle\\(\"Launch Tempo when I sign in\"|Uses the native macOS login item registration for this app\\.|launchAtLoginErrorMessage" Sources/TempoApp/Models/AppSettingsRecord.swift Sources/TempoApp/App/TempoAppModel.swift Sources/TempoApp/Features/Settings/SettingsPopoverView.swift</verify>
  <acceptance_criteria>
    - Sources/TempoApp/Models/AppSettingsRecord.swift contains "var launchAtLoginEnabled: Bool"
    - Sources/TempoApp/App/TempoAppModel.swift contains "func syncLaunchAtLoginPreferenceFromSystem()"
    - Sources/TempoApp/App/TempoAppModel.swift contains "func saveLaunchAtLoginPreference(_ enabled: Bool) throws"
    - Sources/TempoApp/Features/Settings/SettingsPopoverView.swift contains "Section(\"Launch at Login\")"
    - Sources/TempoApp/Features/Settings/SettingsPopoverView.swift contains "Toggle(\"Launch Tempo when I sign in\""
    - Sources/TempoApp/Features/Settings/SettingsPopoverView.swift contains "Uses the native macOS login item registration for this app."
  </acceptance_criteria>
  <done>The existing settings popover can now toggle a real launch-at-login preference while keeping local persistence in sync with system registration.</done>
</task>

<task type="auto">
  <name>Task 3: Add deterministic tests for bootstrap sync, persistence, and failure rollback</name>
  <files>Tests/TempoAppTests/LaunchAtLoginTests.swift, Tests/TempoAppTests/TempoAppBootstrapTests.swift, Sources/TempoApp/App/TempoAppModel.swift</files>
  <read_first>Tests/TempoAppTests/LaunchAtLoginTests.swift, Tests/TempoAppTests/TempoAppBootstrapTests.swift, Sources/TempoApp/App/TempoAppModel.swift, Sources/TempoApp/Features/Settings/SettingsPopoverView.swift, Sources/TempoApp/Support/LaunchAtLoginController.swift</read_first>
  <action>Create Tests/TempoAppTests/LaunchAtLoginTests.swift with fake LaunchAtLoginControlling implementations and tests named `testBootstrapSyncUsesControllerState()`, `testSaveLaunchAtLoginPreferencePersistsEnabledState()`, and `testSaveLaunchAtLoginPreferenceRestoresSystemStateAfterFailure()`. The bootstrap test must initialize TempoAppModel with an in-memory container and a fake controller whose `isEnabled` starts true, then assert `launchAtLoginEnabled` and `settings.launchAtLoginEnabled` are both true after `performInitialLaunchIfNeeded()`. The save test must call `saveLaunchAtLoginPreference(true)` and assert the fake controller recorded `true` plus the settings record persisted true. The failure test must make `setEnabled(_:)` throw, then assert `launchAtLoginEnabled` matches the fake controller's original state and `launchAtLoginErrorMessage` is non-nil. Add a source-level test named `testSettingsPopoverContainsLaunchAtLoginSection()` that reads SettingsPopoverView.swift and asserts the "Launch Tempo when I sign in" copy remains present.</action>
  <verify>rg -n "testBootstrapSyncUsesControllerState|testSaveLaunchAtLoginPreferencePersistsEnabledState|testSaveLaunchAtLoginPreferenceRestoresSystemStateAfterFailure|testSettingsPopoverContainsLaunchAtLoginSection" Tests/TempoAppTests/LaunchAtLoginTests.swift Tests/TempoAppTests/TempoAppBootstrapTests.swift</verify>
  <acceptance_criteria>
    - Tests/TempoAppTests/LaunchAtLoginTests.swift contains "func testBootstrapSyncUsesControllerState()"
    - Tests/TempoAppTests/LaunchAtLoginTests.swift contains "func testSaveLaunchAtLoginPreferencePersistsEnabledState()"
    - Tests/TempoAppTests/LaunchAtLoginTests.swift contains "func testSaveLaunchAtLoginPreferenceRestoresSystemStateAfterFailure()"
    - Tests/TempoAppTests/LaunchAtLoginTests.swift contains "func testSettingsPopoverContainsLaunchAtLoginSection()"
  </acceptance_criteria>
  <done>The launch-at-login feature is covered by deterministic tests instead of relying on the real login-item API during test runs.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `swift test --filter LaunchAtLoginTests` passes
- [ ] `rg -n "LaunchAtLoginControlling|SMAppService.mainApp|launchAtLoginEnabled|Launch Tempo when I sign in" Sources/TempoApp/Support/LaunchAtLoginController.swift Sources/TempoApp/App/TempoAppModel.swift Sources/TempoApp/Features/Settings/SettingsPopoverView.swift` finds the integration contract
- [ ] Enabling or disabling launch at login updates both the system controller and AppSettingsRecord without leaving stale persisted state after failures
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Tempo lets the user enable or disable launch at login from settings
- The launch-at-login state stays synchronized between the native macOS login item registration and local settings persistence
  </success_criteria>

<output>
After completion, create `.planning/phases/06-launch-and-correctness-hardening/06-01-SUMMARY.md`
</output>
