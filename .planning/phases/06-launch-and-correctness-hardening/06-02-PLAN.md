---
phase: 06-launch-and-correctness-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - Sources/TempoApp/App/TempoAppModel.swift
  - Sources/TempoApp/Scheduler/PollingScheduler.swift
  - Sources/TempoApp/Scheduler/SchedulerClock.swift
  - Sources/TempoApp/Scheduler/SchedulerStateStore.swift
  - Sources/TempoApp/Models/SchedulerStateRecord.swift
  - Tests/TempoAppTests/PollingSchedulerTests.swift
  - Tests/TempoAppTests/TempoAppBootstrapTests.swift
  - Tests/TempoAppTests/CheckInCompletionTests.swift
autonomous: true
requirements:
  - SETG-04
must_haves:
  truths:
    - Relaunching Tempo does not inflate elapsed work time with periods when the app was not actively tracking in the foreground.
    - Silence-for-rest-of-day expires correctly at the next local midnight even when the app crosses midnight by relaunching or waking later.
    - The scheduler, app-model bootstrap path, and check-in completion flow agree on the same persisted timing boundaries after restart-related edge cases.
  artifacts:
    - Sources/TempoApp/Scheduler/PollingScheduler.swift
    - Sources/TempoApp/App/TempoAppModel.swift
    - Sources/TempoApp/Models/SchedulerStateRecord.swift
    - Tests/TempoAppTests/PollingSchedulerTests.swift
    - Tests/TempoAppTests/TempoAppBootstrapTests.swift
  key_links:
    - PollingScheduler.swift must remain the canonical owner of elapsed-time and midnight-reset semantics so relaunch, silence, delay, and completion flows cannot drift apart.
    - TempoAppModel.swift must recover persisted scheduler state through one launch-time path so menu-bar copy, prompt presentation, and settings changes all see the same reconciled snapshot.
---

<objective>
Validate and harden scheduler correctness across restart and midnight edge cases.

Purpose: Make the timing engine dependable for daily use by explicitly handling relaunch gaps, expired silence windows, and calendar-boundary recovery instead of relying on incidental current behavior.
Output: Calendar-aware scheduler hardening, app-model relaunch recovery updates, and deterministic regression tests for restart and midnight scenarios.
</objective>

<execution_context>
@/Users/teo/src/tempo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/teo/src/tempo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-launch-and-correctness-hardening/06-01-PLAN.md
@Sources/TempoApp/App/TempoAppModel.swift
@Sources/TempoApp/Scheduler/PollingScheduler.swift
@Sources/TempoApp/Models/SchedulerStateRecord.swift
@Tests/TempoAppTests/PollingSchedulerTests.swift
@Tests/TempoAppTests/TempoAppBootstrapTests.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make PollingScheduler calendar-aware and clamp elapsed time across relaunch gaps</name>
  <files>Sources/TempoApp/Scheduler/PollingScheduler.swift, Sources/TempoApp/Scheduler/SchedulerClock.swift, Sources/TempoApp/Models/SchedulerStateRecord.swift</files>
  <read_first>Sources/TempoApp/Scheduler/PollingScheduler.swift, Sources/TempoApp/Scheduler/SchedulerClock.swift, Sources/TempoApp/Models/SchedulerStateRecord.swift, Tests/TempoAppTests/PollingSchedulerTests.swift</read_first>
  <action>Update PollingScheduler so its initializer accepts `calendar: Calendar = .current` alongside the existing clock and stores it as a property. Replace every `Calendar.current` use inside PollingScheduler with that injected calendar, including `nextLocalMidnight(after:)`. Add a private helper named `effectiveElapsedStart(state:scheduledCheckInAt:pollingInterval:)` that returns the latest of `state.lastCheckInAt`, `state.lastAppLaunchAt`, or `scheduledCheckInAt - pollingInterval`. Use that helper for overdue prompt elapsed calculations and delayed prompt elapsed calculations so the app does not count time spent fully closed between launches as accountable work. When `state.silenceEndsAt` is non-nil and `eventDate >= silenceEndsAt`, clear silence fields and schedule the next check-in from `max(eventDate, silenceEndsAt).addingTimeInterval(pollingInterval)` with `accountableElapsedInterval` reset to exactly one polling interval.</action>
  <verify>rg -n "private let calendar: Calendar|init\\(clock: .*calendar: Calendar = \\.current\\)|effectiveElapsedStart|max\\(eventDate, silenceEndsAt\\)|calendar.startOfDay" Sources/TempoApp/Scheduler/PollingScheduler.swift</verify>
  <acceptance_criteria>
    - Sources/TempoApp/Scheduler/PollingScheduler.swift contains "private let calendar: Calendar"
    - Sources/TempoApp/Scheduler/PollingScheduler.swift contains "init(clock: any SchedulerClock, calendar: Calendar = .current)"
    - Sources/TempoApp/Scheduler/PollingScheduler.swift contains "func effectiveElapsedStart"
    - Sources/TempoApp/Scheduler/PollingScheduler.swift does not contain "Calendar.current"
    - Sources/TempoApp/Scheduler/PollingScheduler.swift contains "max(eventDate, silenceEndsAt)"
  </acceptance_criteria>
  <done>The scheduler now uses one explicit calendar and no longer inflates elapsed time with relaunch downtime or stale silence state.</done>
</task>

<task type="auto">
  <name>Task 2: Route app-model launch and activation flows through the hardened recovery path</name>
  <files>Sources/TempoApp/App/TempoAppModel.swift, Sources/TempoApp/Scheduler/SchedulerStateStore.swift</files>
  <read_first>Sources/TempoApp/App/TempoAppModel.swift, Sources/TempoApp/Scheduler/SchedulerStateStore.swift, Sources/TempoApp/Scheduler/PollingScheduler.swift, Sources/TempoApp/App/TempoApp.swift</read_first>
  <action>In TempoAppModel add an injected `calendar: Calendar = .current` parameter and pass it into PollingScheduler and CSVExportService so launch-time behavior uses one consistent local calendar. Add a helper named `recoverSchedulerState(eventDate:)` that wraps the current `scheduler.updateState(...)`, `schedulerStateStore.apply(...)`, save, `apply(snapshot:)`, and `refreshCheckInPromptState()` sequence. Replace direct transition calls in `performInitialLaunchIfNeeded()`, `handleSceneActivation()`, `handleAppWake()`, and `saveSettings()` with `recoverSchedulerState(eventDate:)`. Preserve the existing idle guard behavior, but when `performInitialLaunchIfNeeded()` runs it must first call `syncLaunchAtLoginPreferenceFromSystem()` from plan 06-01 and then recover scheduler state so startup rehydrates both settings and timing from persisted state in one pass.</action>
  <verify>rg -n "calendar: Calendar = \\.current|recoverSchedulerState\\(eventDate:|syncLaunchAtLoginPreferenceFromSystem\\(|performInitialLaunchIfNeeded\\(|handleSceneActivation\\(|handleAppWake\\(|saveSettings\\(" Sources/TempoApp/App/TempoAppModel.swift</verify>
  <acceptance_criteria>
    - Sources/TempoApp/App/TempoAppModel.swift contains "calendar: Calendar = .current"
    - Sources/TempoApp/App/TempoAppModel.swift contains "func recoverSchedulerState(eventDate: Date)"
    - Sources/TempoApp/App/TempoAppModel.swift contains "syncLaunchAtLoginPreferenceFromSystem()"
    - Sources/TempoApp/App/TempoAppModel.swift contains "recoverSchedulerState(eventDate: clock.now)"
  </acceptance_criteria>
  <done>The app boot and reactivation paths now run through one reconciled scheduler recovery flow instead of duplicating timing state updates.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression tests for relaunch gaps, midnight silence rollover, and recovered prompts</name>
  <files>Tests/TempoAppTests/PollingSchedulerTests.swift, Tests/TempoAppTests/TempoAppBootstrapTests.swift, Tests/TempoAppTests/CheckInCompletionTests.swift</files>
  <read_first>Tests/TempoAppTests/PollingSchedulerTests.swift, Tests/TempoAppTests/TempoAppBootstrapTests.swift, Tests/TempoAppTests/CheckInCompletionTests.swift, Sources/TempoApp/Scheduler/PollingScheduler.swift, Sources/TempoApp/App/TempoAppModel.swift</read_first>
  <action>Extend PollingSchedulerTests with `testOverdueRelaunchClampsElapsedToLastLaunchAt()`, `testDelayedPromptAfterRelaunchUsesLastLaunchAsReferenceStart()`, and `testSilenceExpiredAcrossMidnightSchedulesFromWakeTime()`. These tests must seed SchedulerStateRecord values where `lastAppLaunchAt` is later than `lastCheckInAt`, then assert overdue or delayed elapsed intervals are measured from `lastAppLaunchAt` rather than from hours-old check-in times, and assert an expired `silenceEndsAt` clears to nil while `nextCheckInAt` becomes `eventDate + pollingInterval`. Extend TempoAppBootstrapTests with `testPerformInitialLaunchRecoversExpiredSilenceState()` and `testHandleSceneActivationRecoversOverduePromptAfterRelaunch()` using FixedBootstrapClock dates across midnight and relaunch gaps. Add `testSelectingProjectAfterRecoveredRelaunchSchedulesFreshPollingInterval()` to CheckInCompletionTests to prove a recovered overdue prompt still writes an entry and schedules the next check-in from completion time without carrying the old relaunch gap forward.</action>
  <verify>rg -n "testOverdueRelaunchClampsElapsedToLastLaunchAt|testDelayedPromptAfterRelaunchUsesLastLaunchAsReferenceStart|testSilenceExpiredAcrossMidnightSchedulesFromWakeTime|testPerformInitialLaunchRecoversExpiredSilenceState|testHandleSceneActivationRecoversOverduePromptAfterRelaunch|testSelectingProjectAfterRecoveredRelaunchSchedulesFreshPollingInterval" Tests/TempoAppTests/PollingSchedulerTests.swift Tests/TempoAppTests/TempoAppBootstrapTests.swift Tests/TempoAppTests/CheckInCompletionTests.swift</verify>
  <acceptance_criteria>
    - Tests/TempoAppTests/PollingSchedulerTests.swift contains "func testOverdueRelaunchClampsElapsedToLastLaunchAt()"
    - Tests/TempoAppTests/PollingSchedulerTests.swift contains "func testDelayedPromptAfterRelaunchUsesLastLaunchAsReferenceStart()"
    - Tests/TempoAppTests/PollingSchedulerTests.swift contains "func testSilenceExpiredAcrossMidnightSchedulesFromWakeTime()"
    - Tests/TempoAppTests/TempoAppBootstrapTests.swift contains "func testPerformInitialLaunchRecoversExpiredSilenceState()"
    - Tests/TempoAppTests/TempoAppBootstrapTests.swift contains "func testHandleSceneActivationRecoversOverduePromptAfterRelaunch()"
    - Tests/TempoAppTests/CheckInCompletionTests.swift contains "func testSelectingProjectAfterRecoveredRelaunchSchedulesFreshPollingInterval()"
  </acceptance_criteria>
  <done>The restart and midnight edge cases are protected by deterministic tests that will catch future timing regressions before they reach daily use.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `swift test --filter PollingSchedulerTests` passes
- [ ] `swift test --filter TempoAppBootstrapTests` passes
- [ ] `swift test --filter CheckInCompletionTests` passes
- [ ] Relaunch downtime does not inflate accountable elapsed time, and expired silence windows reset cleanly at local midnight or the next recovery event
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Tempo recovers timing state correctly across relaunches and date boundaries
- Daily use no longer depends on manual bookkeeping to correct restart or midnight timing drift
  </success_criteria>

<output>
After completion, create `.planning/phases/06-launch-and-correctness-hardening/06-02-SUMMARY.md`
</output>
