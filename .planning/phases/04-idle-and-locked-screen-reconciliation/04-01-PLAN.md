---
phase: 04-idle-and-locked-screen-reconciliation
plan: 01
type: execute
wave: 1
depends_on:
  - "03-01"
  - "03-02"
  - "03-03"
files_modified:
  - Sources/TempoApp/App/TempoAppModel.swift
  - Sources/TempoApp/Scheduler/PollingScheduler.swift
  - Sources/TempoApp/Scheduler/SchedulerStateStore.swift
  - Sources/TempoApp/Models/SchedulerStateRecord.swift
  - Sources/TempoApp/Views/MenuBarRootView.swift
  - Tests/TempoAppTests/PollingSchedulerTests.swift
  - Tests/TempoAppTests/TempoAppBootstrapTests.swift
autonomous: true
requirements:
  - IDLE-01
  - IDLE-02
  - IDLE-03
must_haves:
  truths:
    - Tempo detects both keyboard or mouse inactivity past the configured idle threshold and macOS screen-lock transitions.
    - Once an idle or locked interval begins, Tempo stops treating that interval as active accountable work until the user resolves it.
    - Returning from idle exposes a persisted pending idle interval that later plans can present for user resolution.
  artifacts:
    - Sources/TempoApp/Scheduler/PollingScheduler.swift
    - Sources/TempoApp/App/TempoAppModel.swift
    - Sources/TempoApp/Models/SchedulerStateRecord.swift
  key_links:
    - PollingScheduler.swift must remain the canonical owner of accountable elapsed-time semantics so idle, delay, silence, wake, and relaunch paths all converge on one state model.
    - TempoAppModel.swift should own the AppKit and NSWorkspace observers for idle-return and screen-lock events because it already centralizes lifecycle transitions.
---

<objective>
Implement idle and locked-screen detection with suspended active tracking.

Purpose: Stop inactive or locked time from inflating the accountable work block while preserving enough persisted state to reconcile that interval on return.
Output: Scheduler-backed pending idle state, app lifecycle hooks for idle and lock events, menu-bar visibility for unresolved idle intervals, and deterministic tests.
</objective>

<execution_context>
@/Users/teo/src/tempo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/teo/src/tempo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-deferrals-silence-and-menu-bar-control/03-01-SUMMARY.md
@.planning/phases/03-deferrals-silence-and-menu-bar-control/03-02-SUMMARY.md
@.planning/phases/03-deferrals-silence-and-menu-bar-control/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend scheduler state with explicit pending-idle intervals</name>
  <files>Sources/TempoApp/Scheduler/PollingScheduler.swift, Sources/TempoApp/Scheduler/SchedulerStateStore.swift, Sources/TempoApp/Models/SchedulerStateRecord.swift</files>
  <read_first>Sources/TempoApp/Scheduler/PollingScheduler.swift, Sources/TempoApp/Scheduler/SchedulerStateStore.swift, Sources/TempoApp/Models/SchedulerStateRecord.swift, Sources/TempoApp/Models/AppSettingsRecord.swift, Tests/TempoAppTests/PollingSchedulerTests.swift</read_first>
  <action>Add persisted idle-reconciliation fields to SchedulerStateRecord named idleBeganAt, idleDetectedAt, idleResolvedAt, pendingIdleStartedAt, pendingIdleEndedAt, and pendingIdleReason. Extend PollingSchedulerSnapshot and PollingSchedulerResult with isIdlePending plus pendingIdleStartedAt, pendingIdleEndedAt, pendingIdleReason, and accountableWorkEndAt. Add scheduler APIs named beginIdleInterval(state:settings:eventDate:reason:) and resolveReturnedIdleState(state:settings:eventDate:). BeginIdleInterval must set pendingIdleStartedAt to the last active-work boundary, set pendingIdleEndedAt to eventDate, record the passed reason value "inactivity" or "screen-locked", zero accountableElapsedInterval, clear delayedUntilAt and silence fields only when they conflict with the new idle state, and leave nextCheckInAt nil while idle is unresolved. ResolveReturnedIdleState must keep the pending idle interval persisted, restore isIdlePending in the snapshot, and schedule no fresh accountable block until the user resolves the idle interval in plan 04-02.</action>
  <verify>rg -n "idleBeganAt|pendingIdleStartedAt|pendingIdleReason|func beginIdleInterval|func resolveReturnedIdleState|accountableWorkEndAt|isIdlePending" Sources/TempoApp/Scheduler/PollingScheduler.swift Sources/TempoApp/Scheduler/SchedulerStateStore.swift Sources/TempoApp/Models/SchedulerStateRecord.swift</verify>
  <acceptance_criteria>
    - Sources/TempoApp/Models/SchedulerStateRecord.swift contains "pendingIdleStartedAt"
    - Sources/TempoApp/Models/SchedulerStateRecord.swift contains "pendingIdleReason"
    - Sources/TempoApp/Scheduler/PollingScheduler.swift contains "func beginIdleInterval"
    - Sources/TempoApp/Scheduler/PollingScheduler.swift contains "func resolveReturnedIdleState"
    - Sources/TempoApp/Scheduler/PollingScheduler.swift contains "accountableWorkEndAt"
    - Sources/TempoApp/Scheduler/PollingScheduler.swift contains "isIdlePending"
  </acceptance_criteria>
  <done>The scheduler can persist an unresolved idle interval and stop active elapsed-time accumulation until the interval is explicitly reconciled.</done>
</task>

<task type="auto">
  <name>Task 2: Wire inactivity and screen-lock events through the app model</name>
  <files>Sources/TempoApp/App/TempoAppModel.swift, Sources/TempoApp/Views/MenuBarRootView.swift</files>
  <read_first>Sources/TempoApp/App/TempoAppModel.swift, Sources/TempoApp/Views/MenuBarRootView.swift, Sources/TempoApp/Scheduler/PollingScheduler.swift, Sources/TempoApp/Models/AppSettingsRecord.swift, Sources/TempoApp/Models/SchedulerStateRecord.swift</read_first>
  <action>Add observable app-model properties named isIdlePending, pendingIdleStartedAt, pendingIdleEndedAt, pendingIdleReason, and pendingIdleDuration. Add helper methods named detectInactivityIfNeeded(activityDate:), handleScreenLock(), and handleIdleReturn(). DetectInactivityIfNeeded(activityDate:) must compare activityDate against clock.now using settings.idleThresholdMinutes and call PollingScheduler.beginIdleInterval with reason "inactivity" once the threshold is crossed. HandleScreenLock() must call PollingScheduler.beginIdleInterval with reason "screen-locked" at clock.now. HandleIdleReturn() must call PollingScheduler.resolveReturnedIdleState, refresh the prompt state, and keep the unresolved idle interval visible instead of scheduling a normal check-in. Register NSWorkspace session notifications for lock and unlock alongside the existing wake observer, and update MenuBarRootView so when isIdlePending is true it shows a status card whose title is "Idle pending" and whose primary text includes "Resolve idle time".</action>
  <verify>rg -n "isIdlePending|pendingIdleDuration|detectInactivityIfNeeded|handleScreenLock|handleIdleReturn|NSWorkspace.sessionDidBecomeActiveNotification|NSWorkspace.screensDidSleepNotification|Resolve idle time|Idle pending" Sources/TempoApp/App/TempoAppModel.swift Sources/TempoApp/Views/MenuBarRootView.swift</verify>
  <acceptance_criteria>
    - Sources/TempoApp/App/TempoAppModel.swift contains "var isIdlePending"
    - Sources/TempoApp/App/TempoAppModel.swift contains "func detectInactivityIfNeeded"
    - Sources/TempoApp/App/TempoAppModel.swift contains "func handleScreenLock"
    - Sources/TempoApp/App/TempoAppModel.swift contains "func handleIdleReturn"
    - Sources/TempoApp/App/TempoAppModel.swift contains "NSWorkspace.sessionDidBecomeActiveNotification"
    - Sources/TempoApp/Views/MenuBarRootView.swift contains "Idle pending"
    - Sources/TempoApp/Views/MenuBarRootView.swift contains "Resolve idle time"
  </acceptance_criteria>
  <done>Tempo listens for the concrete idle and lock lifecycle events, persists the unresolved interval, and surfaces that blocked state in the menu bar.</done>
</task>

<task type="auto">
  <name>Task 3: Add tests for idle detection, lock transitions, and suspended elapsed time</name>
  <files>Tests/TempoAppTests/PollingSchedulerTests.swift, Tests/TempoAppTests/TempoAppBootstrapTests.swift, Sources/TempoApp/App/TempoAppModel.swift</files>
  <read_first>Tests/TempoAppTests/PollingSchedulerTests.swift, Tests/TempoAppTests/TempoAppBootstrapTests.swift, Sources/TempoApp/App/TempoAppModel.swift, Sources/TempoApp/Scheduler/PollingScheduler.swift, Sources/TempoApp/Models/SchedulerStateRecord.swift</read_first>
  <action>Extend PollingSchedulerTests with tests named testBeginIdleIntervalSuspendsAccountableElapsed(), testScreenLockCreatesPendingIdleInterval(), and testResolveReturnedIdleStateKeepsPendingIdleUnscheduled() that prove pending idle intervals persist with reason values "inactivity" and "screen-locked" and that nextCheckInAt remains nil until reconciliation. Add bootstrap tests named testDetectInactivityIfNeededMarksIdlePending() and testHandleScreenLockMarksIdlePending() that seed an overdue active interval, invoke the new app-model methods, and assert checkInPromptState.isPresented is false, accountableElapsedInterval is 0, and pendingIdleDuration equals the expected idle window length.</action>
  <verify>rg -n "testBeginIdleIntervalSuspendsAccountableElapsed|testScreenLockCreatesPendingIdleInterval|testResolveReturnedIdleStateKeepsPendingIdleUnscheduled|testDetectInactivityIfNeededMarksIdlePending|testHandleScreenLockMarksIdlePending" Tests/TempoAppTests/PollingSchedulerTests.swift Tests/TempoAppTests/TempoAppBootstrapTests.swift</verify>
  <acceptance_criteria>
    - Tests/TempoAppTests/PollingSchedulerTests.swift contains "func testBeginIdleIntervalSuspendsAccountableElapsed()"
    - Tests/TempoAppTests/PollingSchedulerTests.swift contains "func testScreenLockCreatesPendingIdleInterval()"
    - Tests/TempoAppTests/PollingSchedulerTests.swift contains "func testResolveReturnedIdleStateKeepsPendingIdleUnscheduled()"
    - Tests/TempoAppTests/TempoAppBootstrapTests.swift contains "func testDetectInactivityIfNeededMarksIdlePending()"
    - Tests/TempoAppTests/TempoAppBootstrapTests.swift contains "func testHandleScreenLockMarksIdlePending()"
  </acceptance_criteria>
  <done>The idle-detection path is protected by deterministic scheduler and app-model tests that verify inactive time no longer counts as active work.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Crossing the configured idle threshold or locking the screen creates a persisted pending idle interval
- [ ] While idle is pending, accountable elapsed time is zeroed and no fresh next-check-in countdown is scheduled
- [ ] Returning from idle keeps the unresolved interval visible instead of silently resuming active tracking
- [ ] The menu bar shows an explicit unresolved-idle state with "Resolve idle time" copy
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Tempo detects inactivity and screen locks and stops counting that interval as active work by default
- An unresolved idle block persists across wake, activation, and relaunch until the user intentionally reconciles it
  </success_criteria>

<output>
After completion, create `.planning/phases/04-idle-and-locked-screen-reconciliation/04-01-SUMMARY.md`
</output>
