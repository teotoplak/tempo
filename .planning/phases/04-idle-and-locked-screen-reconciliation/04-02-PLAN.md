---
phase: 04-idle-and-locked-screen-reconciliation
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
  - "02-03"
files_modified:
  - Sources/TempoApp/App/TempoAppModel.swift
  - Sources/TempoApp/Features/CheckIn/CheckInPromptContent.swift
  - Sources/TempoApp/Features/CheckIn/CheckInProjectListView.swift
  - Sources/TempoApp/Models/TimeEntryRecord.swift
  - Tests/TempoAppTests/CheckInCompletionTests.swift
  - Tests/TempoAppTests/PersistenceModelTests.swift
  - Tests/TempoAppTests/TempoAppBootstrapTests.swift
autonomous: true
requirements:
  - IDLE-04
  - IDLE-05
  - IDLE-03
must_haves:
  truths:
    - When the user returns from inactivity, Tempo presents the idle interval and does not hide it until the user assigns, discards, or splits that interval.
    - The user can assign the full idle interval to one project, discard the full interval, or split it into two contiguous segments with independent project assignment.
    - The resulting time ledger remains contiguous and internally consistent, with no overlaps, negative durations, or unaccounted idle minutes.
  artifacts:
    - Sources/TempoApp/App/TempoAppModel.swift
    - Sources/TempoApp/Features/CheckIn/CheckInPromptContent.swift
    - Tests/TempoAppTests/CheckInCompletionTests.swift
  key_links:
    - TempoAppModel.swift must own idle-resolution mutations so scheduler state, prompt presentation, and TimeEntryRecord writes stay atomic.
    - CheckInPromptContent.swift is the canonical UI for prompting the user to resolve idle time before ordinary check-in classification resumes.
---

<objective>
Build the idle-resolution flow for assign, discard, and split actions.

Purpose: Let the user intentionally reconcile unresolved idle time and resume ordinary check-ins without corrupting the tracked-time ledger.
Output: Prompt UI for idle resolution, app-model actions for assign/discard/split, idle-specific time-entry writes, and deterministic consistency tests.
</objective>

<execution_context>
@/Users/teo/src/tempo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/teo/src/tempo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-deferrals-silence-and-menu-bar-control/03-03-SUMMARY.md
@.planning/phases/04-idle-and-locked-screen-reconciliation/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add app-model resolution APIs for assign, discard, and split</name>
  <files>Sources/TempoApp/App/TempoAppModel.swift, Sources/TempoApp/Models/TimeEntryRecord.swift</files>
  <read_first>Sources/TempoApp/App/TempoAppModel.swift, Sources/TempoApp/Models/TimeEntryRecord.swift, Sources/TempoApp/Models/ProjectRecord.swift, Sources/TempoApp/Models/SchedulerStateRecord.swift, Tests/TempoAppTests/CheckInCompletionTests.swift</read_first>
  <action>Add an idle-entry source contract in TimeEntryRecord by writing idle reconciliation rows with source values "idle-assigned" and "idle-split". In TempoAppModel add methods named assignPendingIdle(to:), discardPendingIdle(), and splitPendingIdle(firstProject:firstDurationMinutes:secondProject:). AssignPendingIdle(to:) must insert one TimeEntryRecord from pendingIdleStartedAt to pendingIdleEndedAt for the chosen project, clear the pending idle fields, and schedule the next check-in from clock.now plus settings.pollingIntervalMinutes. DiscardPendingIdle() must clear the same pending idle fields without inserting any TimeEntryRecord and still schedule the next normal poll from clock.now. SplitPendingIdle(firstProject:firstDurationMinutes:secondProject:) must require firstDurationMinutes be greater than 0 and less than the total idle minutes, create two contiguous TimeEntryRecord rows that exactly partition the pending idle interval, mark both rows with source "idle-split", and reject any split whose two segments do not sum to pendingIdleEndedAt.timeIntervalSince(pendingIdleStartedAt).</action>
  <verify>rg -n "idle-assigned|idle-split|func assignPendingIdle|func discardPendingIdle|func splitPendingIdle|firstDurationMinutes" Sources/TempoApp/App/TempoAppModel.swift Sources/TempoApp/Models/TimeEntryRecord.swift</verify>
  <acceptance_criteria>
    - Sources/TempoApp/App/TempoAppModel.swift contains "func assignPendingIdle"
    - Sources/TempoApp/App/TempoAppModel.swift contains "func discardPendingIdle"
    - Sources/TempoApp/App/TempoAppModel.swift contains "func splitPendingIdle"
    - Sources/TempoApp/App/TempoAppModel.swift contains "firstDurationMinutes"
    - Sources/TempoApp/Models/TimeEntryRecord.swift contains "source: String"
    - Sources/TempoApp/App/TempoAppModel.swift contains "\"idle-assigned\""
    - Sources/TempoApp/App/TempoAppModel.swift contains "\"idle-split\""
  </acceptance_criteria>
  <done>Tempo can resolve an unresolved idle interval into explicit ledger writes or an intentional discard without leaving scheduler state half-cleared.</done>
</task>

<task type="auto">
  <name>Task 2: Present the idle-resolution UI before ordinary project check-in</name>
  <files>Sources/TempoApp/Features/CheckIn/CheckInPromptContent.swift, Sources/TempoApp/Features/CheckIn/CheckInProjectListView.swift, Sources/TempoApp/App/TempoAppModel.swift</files>
  <read_first>Sources/TempoApp/Features/CheckIn/CheckInPromptContent.swift, Sources/TempoApp/Features/CheckIn/CheckInProjectListView.swift, Sources/TempoApp/App/TempoAppModel.swift, Sources/TempoApp/Models/ProjectRecord.swift</read_first>
  <action>Update CheckInPromptState and TempoAppModel prompt-refresh logic so unresolved idle intervals present an idle-resolution variant before the standard "What are you currently doing" check-in. In CheckInPromptContent, render a dedicated section whose header includes the exact text "Resolve idle time", shows the pending idle duration plus the reason label "Inactive" or "Screen locked", and offers three actions: "Assign all to selected project", "Discard idle time", and "Split idle time". Reuse the existing project list for the selected project. For split mode, add a numeric control labeled "First segment (minutes)" plus a second-project picker that defaults to the same project list ordering. Do not allow ordinary check-in completion until the pending idle interval has been cleared successfully.</action>
  <verify>rg -n "Resolve idle time|Assign all to selected project|Discard idle time|Split idle time|First segment \\(minutes\\)|Screen locked|Inactive" Sources/TempoApp/Features/CheckIn/CheckInPromptContent.swift Sources/TempoApp/App/TempoAppModel.swift</verify>
  <acceptance_criteria>
    - Sources/TempoApp/Features/CheckIn/CheckInPromptContent.swift contains "Resolve idle time"
    - Sources/TempoApp/Features/CheckIn/CheckInPromptContent.swift contains "Assign all to selected project"
    - Sources/TempoApp/Features/CheckIn/CheckInPromptContent.swift contains "Discard idle time"
    - Sources/TempoApp/Features/CheckIn/CheckInPromptContent.swift contains "Split idle time"
    - Sources/TempoApp/Features/CheckIn/CheckInPromptContent.swift contains "First segment (minutes)"
    - Sources/TempoApp/App/TempoAppModel.swift contains "Screen locked"
    - Sources/TempoApp/App/TempoAppModel.swift contains "Inactive"
  </acceptance_criteria>
  <done>The user sees a concrete idle-resolution workflow in the prompt and cannot accidentally bypass it with a normal check-in selection.</done>
</task>

<task type="auto">
  <name>Task 3: Add ledger-consistency tests for idle resolution outcomes</name>
  <files>Tests/TempoAppTests/CheckInCompletionTests.swift, Tests/TempoAppTests/PersistenceModelTests.swift, Tests/TempoAppTests/TempoAppBootstrapTests.swift, Sources/TempoApp/App/TempoAppModel.swift</files>
  <read_first>Tests/TempoAppTests/CheckInCompletionTests.swift, Tests/TempoAppTests/PersistenceModelTests.swift, Tests/TempoAppTests/TempoAppBootstrapTests.swift, Sources/TempoApp/App/TempoAppModel.swift, Sources/TempoApp/Models/TimeEntryRecord.swift</read_first>
  <action>Add completion tests named testAssignPendingIdleCreatesIdleAssignedEntry(), testDiscardPendingIdleClearsPendingStateWithoutWritingEntry(), and testSplitPendingIdleCreatesContiguousEntries() that seed pendingIdleStartedAt and pendingIdleEndedAt, invoke the new actions, and assert the resulting entries exactly span the original idle interval with source values "idle-assigned" or "idle-split". Add a persistence test named testSplitPendingIdleRejectsOutOfRangeDuration() that proves splitPendingIdle throws when firstDurationMinutes is 0 or equal to the full idle duration. Add a bootstrap or source-level test named testIdleResolutionPromptBlocksStandardCheckInCopy() that verifies the prompt source contains "Resolve idle time" and that normal completion copy is not the first headline while idle is pending.</action>
  <verify>rg -n "testAssignPendingIdleCreatesIdleAssignedEntry|testDiscardPendingIdleClearsPendingStateWithoutWritingEntry|testSplitPendingIdleCreatesContiguousEntries|testSplitPendingIdleRejectsOutOfRangeDuration|testIdleResolutionPromptBlocksStandardCheckInCopy" Tests/TempoAppTests/CheckInCompletionTests.swift Tests/TempoAppTests/PersistenceModelTests.swift Tests/TempoAppTests/TempoAppBootstrapTests.swift</verify>
  <acceptance_criteria>
    - Tests/TempoAppTests/CheckInCompletionTests.swift contains "func testAssignPendingIdleCreatesIdleAssignedEntry()"
    - Tests/TempoAppTests/CheckInCompletionTests.swift contains "func testDiscardPendingIdleClearsPendingStateWithoutWritingEntry()"
    - Tests/TempoAppTests/CheckInCompletionTests.swift contains "func testSplitPendingIdleCreatesContiguousEntries()"
    - Tests/TempoAppTests/PersistenceModelTests.swift contains "func testSplitPendingIdleRejectsOutOfRangeDuration()"
    - Tests/TempoAppTests/TempoAppBootstrapTests.swift contains "func testIdleResolutionPromptBlocksStandardCheckInCopy()"
  </acceptance_criteria>
  <done>The idle-resolution path is covered by deterministic tests that prove the resulting ledger is complete, contiguous, and intentionally classified.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Returning from idle presents "Resolve idle time" before a normal check-in can be completed
- [ ] Assigning idle time writes one explicit idle entry covering the full unresolved interval
- [ ] Discarding idle time clears the pending state without creating a TimeEntryRecord
- [ ] Splitting idle time writes exactly two contiguous segments whose durations sum to the original idle interval
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Tempo gives the user explicit assign, discard, and split controls for unresolved idle time
- Idle-resolution writes preserve a consistent local time ledger with no gaps or overlaps inside the reconciled interval
  </success_criteria>

<output>
After completion, create `.planning/phases/04-idle-and-locked-screen-reconciliation/04-02-SUMMARY.md`
</output>
